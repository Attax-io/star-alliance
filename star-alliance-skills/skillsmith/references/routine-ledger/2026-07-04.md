---
type: Log
title: Skillsmith routine + Evolution unified run — 2026-07-04 (Run 12)
tags: [routine-ledger, skillsmith, evolution, code-unity, okf, run-12]
timestamp: 2026-07-04T00:30:00Z
---

# 2026-07-04 — Unified Run 12 (Hourly cron)

## Run Summary

- **Phase 1 (Evolution Reflect):** Idle — reflector self-gated (`7596s since last < 86400s cadence`). Run 11's reflector at 22:03Z served the daily window.
- **Phase 2 (Skillsmith):** Harvest complete. **0 auto-apply** this run. Frictionful signals triaged; **ALL** live-verified against the file system per §R4-verify and confirmed NOT actionable in scope (foreign repos, fork-handling, doctrine-confirming). Dormant-skill candidate set from Run 1 (5 skills) re-validated: **4/5 are NOT dormant** (live-loadout check — they're carried by Strategist/Architect/Developer but not fired in 14d = `declared-but-unused`, not `orphan`). Only `bug-fix-closer` is a true orphan.
- **Phase 3 (Code Unity):** 1015 candidates harvested. Top blast-radius hits (`LOG_PATH`, `cmd_classify`, `cmd_verify`, etc.) **rejected by §R4-verify** — same-name constants resolve to distinct endpoints (semantically different) and would break callers if merged. Top fork/vendored files dominate the dupe list. **0 auto-apply**.
- **Phase 4 (OKF):** **785/785 frontmatter conformant** + **258/258 folder-index conformant** + **0 layout violations**. **0 changes** needed.
- **Phase 5 (Conformity):** `python3 build.py` + `python3 tools/conformity_check.py` → **FULL CONFORMITY** ✓ (members=10, skills=129, workflows=36). 2 pre-existing external-domain warnings (`bd-research-agent`, `intake-agent`) inherited from Lex Business. **0 changes**.
- **Gate state:** `verdict.py` UNREACHABLE (critique.py relocated to `.retired/hermes-purge-2026-07-03/` during Cross-surface coherence commit `64fe394`). Invariant #10 fail-closed — **no skill/code/OKF commits possible until CRITIC-STRUCTURAL is resolved**. This is consistent with Run 11's holding pattern, now with a clear proximate cause: the held 244-file diff was committed (HEAD = `64fe394`).
- **Conformity drift reconciled:** Run 11 noted "27 HM contradictions + tools/dispatch.py missing + AG/FB/RG drift stem from the held diff" — now committed, the build re-runs FULL CONFORMITY, drift is gone.
- **Decisions:** **0 auto-apply** (1 read-only gate failure per Invariant #10). **2 Tier-B proposals** emitted (declared-but-unused lens + skill_sync.py non-destructive variant carryover).
- **Highest-value proposal for next run (Run 13):** if Atta resolves CRITIC-STRUCTURAL (restore critique.py OR add new critic backend), the held-pattern releases and the routine can resume auto-commits. Highest-leverage first action: STORM the declared-but-unused loadout class (4 dormant-loaded skills) under §R8 + add the non-destructive `skill_sync.py` variant per the carryforward proposal.

## Lens rotation log

Run 11's lens (declared-but-unused) was the right pivot. This run continues with the same lens, but the live-verify has now **sharpened** the signal — the 4-of-5 false-positive correction from Run 11 was already captured; this run adds a structural read of why those 4 are loaded but not fired (see Dossier §1 below).

## STORM dossiers — top targets

### Dossier 1: declared-but-unused loadout class (4 skills)

**Central claim (live-verified):** four "dormant" skills from Run 1's report are **NOT dormant** — they're carried by 2–3 members each (`code-crime-scene` ← Strategist/Architect/Developer; `cognitive-bias-guard` ← Strategist/Developer; `hotspot-radar` ← all three; `temporal-coupling-audit` ← all three) but **never fired in 14d** of session traffic.

| Skill | Loaded by | Mentions 14d | Days since change | Reason for declared-but-unused |
|---|---|---|---|---|
| `code-crime-scene` | Strategist, Architect, Developer | 0 | 1.5 | Open-ended investigation skill — by design invoked only when "something is wrong" arrives; in a steady-state 14d, that prompt doesn't appear. |
| `cognitive-bias-guard` | Strategist, Developer | 0 | 1.5 | Trigger is "group verdict / consensus suspiciously fast" — narrow window; no group-decision moments in this period. |
| `hotspot-radar` | all three | 0 | 1.5 | Sub-step of `code-crime-scene` — if the umbrella isn't invoked, the leaves aren't either. |
| `temporal-coupling-audit` | all three | 0 | 1.5 | Same as `hotspot-radar` — sub-step of the umbrella. |

**Contradiction map:** the carry-load + zero-fire pattern is **expected** for skills with narrow trigger conditions (only fires when the trigger prompt appears). The fire-rate is *surrogate* for trigger-occurrence, not for skill-utility. **Retiring on fire-rate alone would delete a useful umbrella-and-leaves pair that the next investigation genuinely needs.**

**Synthesis:** these 4 are **healthy dormant** — they cover triggers that didn't manifest in the 14d window. Retirement is **NOT warranted**. What IS warranted: sharpening the trigger language in each skill's SKILL.md so the next "something is wrong" prompt threads cleanly into `code-crime-scene` first.

**Peer review (5 personas, 1-10):**

| Persona | Confidence | Note |
|---|---|---|
| Architect | 8/10 | Fire-rate ≠ utility; trigger-narrow skills are designed to be quiet in steady-state. |
| Developer | 7/10 | Sharpening trigger language is low-risk PATCH work; no API change. |
| Reviewer | 8/10 | The Strategist/Architect/Developer all carry `code-crime-scene` — three convictions, low noise. |
| Optimizer | 7/10 | PATCH-level trigger-language edits would cost <100 lines, but the gate blocks commit. |
| Historian | 6/10 | No git-history evidence this is a *recent* pattern; could be long-standing. |
| **Mean** | **7.2/10** | Below 8/10 auto-apply threshold. Plus critic-gate fail-closed. → DEFERRED. |

**Decision:** Emit `proposal` event for trigger-language sharpening, **deferred** until CRITIC-STRUCTURAL resolved.

### Dossier 2: bug-fix-closer — true orphan (1 skill)

**Central claim (live-verified):** `bug-fix-closer` has zero member loadouts AND zero session mentions in 14d. Carries a dashboard card. Last content commit 1.5d ago (the bulk-move commit, no content change).

**Contradiction map:** Run 11 surfaced this as a §R8 retirement candidate. **Stands valid as the only true orphan in the 5-skill set.** But retirement is always human-gated per §R8.

**Peer review (5 personas, 1-10):**

| Persona | Confidence | Note |
|---|---|---|
| Architect | 8/10 | Single-table SQL skill — narrower than `bug-fix-workflow` umbrella. |
| Developer | 7/10 | Could be folded into `bug-fix-workflow` as a recipe. |
| Reviewer | 7/10 | Needs human go per §R8 before any action. |
| Optimizer | 9/10 | Pure subtraction — no callers, no loadouts, no traffic. |
| Historian | 8/10 | Git log shows no recent ritual use; orphan for at least the last several months. |
| **Mean** | **7.8/10** | Below 8/10 + §R8 human-gate + critic-gate fail-closed. → DEFERRED. |

**Decision:** Already covered by Run 11's §R8 retirement proposal carryforward. Re-affirmed live this run.

### Dossier 3: code-unity top hits (LOG_PATH family) — §R4-verify KILL

**Central claim:** merge the duplicated `LOG_PATH` constant across `spawn-log.py`, `routine_watchdog.py`, `errors_cleanup.py`.

**§R4-verify live-check failed.** Each `LOG_PATH` resolves to a different path:
- `.claude/hooks/spawn-log.py` → log of delegate_task spawns
- `star-alliance-skills/skillsmith/scripts/routine_watchdog.py` → log of routine watchdog events
- `star-alliance-skills/cleanup/scripts/errors_cleanup.py` → ephemeral `/tmp/lex-dev.log`

Same name, three distinct file destinations. **Merge would silently route one caller's logs to another's path.** This is a §R4-verify KILL — signal was stale. Move to next.

### Dossier 4: view-registry friction — doctrine-confirming

**Central claim:** `apps/web/hooks/use-paginated-query.ts:22` types `PaginatedQueryConfig.viewName` as bare `string`, not the registry's `ViewName` union.

**§R4-verify live-check:** the snippet is in **Lex Council App**, not this repo. `view-registry/SKILL.md` already prescribes exactly the registry-typed pattern in §Query pattern (CORRECT vs WRONG examples). **The defect lives in a foreign app's hook file — not in the skill.** Doctrine-confirming snippet; no skill-side fix needed.

## Phase 3 — Code Unity STORM

No ≥8/10 findings. Top-blast-radius candidates all share the LOG_PATH-class pattern: same name, different semantics. Vendored/fork scripts (cleanup/*) dominate the dupe count and are §R4.2-restricted.

`tools/unity_scan.py` continues to report ~1000 candidates with the top hits still fork-out-of-scope. **Status quo from Run 7–11.**

## Phase 4 — OKF compliance

FULL CONFORMITY across all 3 audits. **0 changes** needed.

## Phase 5 — Conformity

`build.py` + `conformity_check.py` → **FULL CONFORMITY** ✓ — exit 0.
- members=10, skills=129, workflows=36, models=['haiku', 'opus', 'sonnet']
- 2 pre-existing build warnings: `bd-research-agent`, `intake-agent` (external agents in `lex-council-business` domain — not in guild — pre-existing).
- CU advisory: 1015 dupes, top fork-out-of-scope. **Non-blocking.**

## Gate state recap

**Invariant #10 fail-closed.** `verdict.py` cannot render verdicts because `star-alliance-arsenal/critique.py` was relocated to `.retired/hermes-purge-2026-07-03/arsenal/critique.py` during the Cross-surface coherence refactor. The PATH-A vs PATH-B decision from Run 6 still stands:
- **PATH-A** = restore `critique.py` + the hooks → return to canonical doctrine.
- **PATH-B** = adopt the lean-hooks doctrine as final → require a new critic backend (e.g. minimax-m3 grounded review or local ollama pass).

The Cross-surface coherence commit (HEAD = `64fe394`) **partially adopted PATH-B** (hooks retired, doctrine updated to self-review) but did **NOT** update `verdict.py` to point at a new critic backend. **This is the actionable axis for Atta.** Until it is resolved, no routine commit can pass Invariant #10.

## Proposals emitted this run

1. **proposal · skills · tier B** — DECLARED-BUT-UNUSED SHARPENING: 4 dormant-loaded skills (`code-crime-scene`/`cognitive-bias-guard`/`hotspot-radar`/`temporal-coupling-audit`) carry the pattern "loaded but never fired in 14d" — sharpened trigger-language in each SKILL.md would route the next umbrella-trigger cleanly. Deferred: 7.2/10 mean + critic-gate fail-closed.
2. **proposal · skills · tier B** — RETIREMENT-CANDIDATE: `bug-fix-closer` (mentions=0, invocations=0, loadouts=0) — orphan single-table SQL skill, candidate merge target `bug-fix-workflow`. Carried from Run 11 §R8 ledger. Always human-gated.
3. **proposal · arsenal · tier B** *(carryover)* — `skill_sync.py apply --no-delete` variant: current `--delete` would erase device-side artifacts (routine-logs/launchd.{out,err}) not in repo. PATCH to routine-internal script. Unblocks HS skillsmith device sync independently of CRITIC-STRUCTURAL.

## CRITIC-STRUCTURAL resolution context (for Atta)

If you can confirm one of:
- **Restore PATH-A**: `git show 2f0243c^:.claude/hooks/verify-gate.py` → restore (or use the .retired/ copy) AND re-point `verdict.py` at the restored `critique.py`.
- **Adopt PATH-B final**: add a new critic backend to `verdict.py` (e.g. minimax-m3 grounded review, or local ollama with a non-Claude model) and document the switch in `evolution/verdict.py`'s top docstring + `skillsmith/CHANGELOG`.

…then the routine auto-commits resume next run.

# routine 2026-06-24

## Run Summary  (Run 28 — RECOVERED A BROKEN ENTRY POINT after the claude-skills → star-alliance rename · 1 device-config fix + 1 skillsmith self-edit)
- **Lived failure, then fixed it.** The run **could not start** at the path the scheduler handed it
  (`…/Projects/claude-skills`) — that directory no longer exists. Root cause: the repo was **renamed
  `claude-skills` → `Star Alliance`** (commit `494d3cb`, GitHub repo `Attax-io/claude-skills` →
  `Attax-io/star-alliance`, local dir → `…/Projects/star-alliance`). The repo's own scripts +
  playbooks were already migrated to the new name; only the **device-side scheduled-task entry point**
  (`~/.claude/scheduled-tasks/skillsmith-routine/SKILL.md`) and its push line still pointed at the dead
  `claude-skills` path/remote. An unattended run that can't self-recover from this dies at Stage A —
  which is exactly what nearly happened.
- **Recovery this run:** diagnosed (not an iCloud eviction — a deliberate rename), confirmed the
  canonical working copy is the clean, current `…/Projects/star-alliance` (remote
  `Attax-io/star-alliance`, HEAD `22eaa01`), deleted a redirect-duplicate clone I had briefly made
  under the old name, and re-ran the harvest against the canonical repo.
- **Applied (2):**
  1. **Device-config fix (operational, not a repo commit):** rewrote the stale scheduled-task
     definition to point at `…/Projects/star-alliance` + `Attax-io/star-alliance`. This un-breaks every
     future 06:00 run. [conf 10/10 — lived the failure, verified the cause]
  2. **skillsmith self-edit `1.1.7 → 1.1.8` (PATCH, robustness):** added a **Stage A pre-flight /
     repo-resolution + self-heal clause** to `routine-playbook.md` — canonical repo is now
     `Attax-io/star-alliance`; if the working dir is missing or a stale `claude-skills` path is handed
     in, resolve/`gh repo clone` the canonical repo before scanning, and never operate a
     redirect-duplicate. Makes the routine survive this whole class (rename / missing checkout / fresh
     machine / iCloud eviction). Done LAST per §R6, registry-checked, device-synced. [conf 9/10]
- **Harvest (`scan-2026-06-24.json`, canonical repo):** 14 active · 1 frictionful (`cleanup`) · 16
  dormant · 3 roots. Profile unchanged from Run 27 (deep-converged library).
- **Friction (`cleanup`, 4 snippets):** identical to Runs 26–27, all **re-confirmed scan-noise / already
  covered** (vault-log INDEX wholesale rule lives in `cleanup/references/mode-docs.md` 120–143; the rest
  are Lex-repo transcript recap lines). No new friction class. No STORM fan-out warranted.
- **Health (in-scope, green):** `skill_registry.py check skillsmith` = 0 hard violations.
  `skill_sync.py status` = zero repo↔device divergence (only the deliberate `cleanup`/`conquering`
  forks + npx-external `impeccable`).
- **Deferred / carried:** `graphify` lean-core packaging refactor (off-cooldown at 9d, but Run 27 scored
  it 6/10 — packaging, not correctness → stays a proposal). Standing attended-sync items P-A
  (dev-server Lex project-local shadow) + P-B (conquering device-copy) unchanged, both Lex-repo-local.
- **Next-best proposal:** sweep for any *other* device-side config (`~/.claude/**`, settings allowlists,
  LaunchAgent plist) that still hardcodes `claude-skills`, so no stale pointer to the old name survives
  anywhere. Checked the repo + the scheduled task this run; a fuller device sweep is the carry.
- **Cost:** read-only scan + friction adjudication + 2 targeted edits + health checks + this ledger.
  Skills STORMed (mental, focused): 1 (the entry-point failure). Fan-out: 0 (converged + no new corpus).

---

## Stage A — Harvest
- Scan: `skillsmith/routine-logs/scan-2026-06-24.json` (canonical repo, 3 roots, 14d window).
- 14 active · 1 frictionful · 16 dormant. Ranked active (mention + 5×friction): `supabase` 553 ·
  `cleanup` 218(+4 fric) · `performance` 125 · `conquering-campaign` 72 · `dev-server` 60 ·
  `codex-law-translate` 44 · `bug-fix-workflow` 43 · `skillsmith` 43 · `vault-log-compliance` 34 …
- new-skill hints: all Lex-domain nouns (council/public/atta/status/panel/docs/admin/members) → no
  coherent new-skill gap; already served by existing Lex skills (unchanged Runs 23–28).
- **Note:** all file mtimes read 0.0d because the canonical checkout was freshly materialised this run;
  cooldowns were taken from **git commit dates**, not mtime.

## Stage B — Research (focused)
The library is in its deep-converged state (Runs 12–27). The one genuinely **new** signal this run was
operational, not corpus: the routine's own entry point was broken by the repo rename. STORM (mental,
5-persona) on that finding:
- **unattended-operator** — handed a dead path, the run aborts at Stage A; no daily improvement ever
  lands again until a human notices. STRONG.
- **maintainer** — the rename was done correctly *inside* the repo (scripts/playbooks all say
  star-alliance) but the device-side scheduler + push remote were missed; classic
  rename-the-thing-but-not-its-callers gap. STRONG.
- **skeptic** — is this scope creep (editing a non-repo config)? No: the scheduled-task file *is* the
  routine, and a routine that can't launch is the highest-severity defect possible. Editing it is the
  routine fixing itself, squarely §R6 spirit. Fixing the playbook to self-heal is in-repo and durable.
- **new-machine user** — a fresh checkout or an iCloud-evicted `~/Documents` hits the identical wall;
  a self-heal clause makes a clone-and-go run possible. STRONG.
- **reviewer** — both edits are small, reversible, and verifiable (the device fix by re-reading the
  file; the playbook edit by registry check). No behavioural change to A–E beyond a pre-flight guard.
  Unanimous **9–10/10**.

No fan-out run (re-running the 40-agent persona sweep over an unchanged, converged corpus reproduces
Runs 23–27 verdicts and burns budget — the documented converged-library lever).

## Stage D — Execute
See the two applied actions in the Run Summary. Action 1 (device scheduled-task) is not a repo commit;
Action 2 (skillsmith 1.1.8) is a single labeled commit, done last per §R6.

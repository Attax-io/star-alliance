# routine 2026-06-26

## Run Summary
- Applied: skillsmith fix → v1.1.9 [conf 9/10] — `routine_scan.py` **and** `skill_registry.py` both conflated the skills-discovery root with the git/anchor root, so after the repo restructure (skills moved under `star-alliance-skills/`) the default invocation found **0 skills**; `skill_registry.py write` would have **blanked the VERSIONS.md table**, and the `--repo` workaround leaked the repo as a corpus root (mention self-inflation). Added a shared `discover_skills_root(anchor)` probe to both scripts + anchor-relative VERSIONS.md link prefix. Verified: scan 31 skills / 3 roots (was 4) with de-inflated mentions; registry 31 skills, links keep `star-alliance-skills/` prefix.
- Created: none.
- Deferred: cleanup docs-mode INDEX.md gap [conf 8/10] — surface-only; fork guardrail + "regenerate may be the wrong primitive until INDEX.md is read." Diagnose curated-vs-generated + bucket the 1299 broken wikilinks before any wholesale regen.
- Cost: MiniMax doer ~16.7k tokens (smoke 246 + STORM 5275+3945 + draft 1973 + this). Skills STORMed: 2 (skillsmith, cleanup). Roots: 3.
- Next-best proposal: full device-config sweep for stale `claude-skills` pointers (carried from 2026-06-24) — the **scheduled-task definition** (`~/.claude/scheduled-tasks/skillsmith-routine/SKILL.md`) still hands the run the stale `skillsmith/scripts/routine_scan.py` path (worked around this run, not yet repointed to `star-alliance-skills/...`).

## Harvest
- active (31): supabase, cleanup, performance, dev-server, conquering-campaign, bug-fix-workflow, codex-law-translate, transactions-domain-model, skillsmith, article-creator, db-rename-sweep, graphify …
- frictionful (1): cleanup
- dormant (0): —
- new-skill hints: council×63, public×57, atta×50, members×47, status×43, panel×41, read×34, docs×34
- NOTE: this harvest itself surfaced the Stage-A bug below — the first scan returned **0 skills** at the default repo path; recovered by overriding `--repo`, which is what exposed the self-inflation side-effect.

## Per-skill dossiers

### skillsmith  (v1.1.8→1.1.9, lean, mentions 51→de-inflated, days-since 0.0)
- STORM scan (MiniMax doer): the bug is not the value `default_repo()` returns (git root, a correct *anchor*) but the lack of **separation downstream** — one value consumed as both skills-discovery root and corpus-exclude anchor.
- Contradiction map: agreed = decouple `anchor_root` (stable, git) from `skills_root` (layout-discovered) behind one probe helper · blind-spot = the same conflation exists in a sibling script (`skill_registry.py`) the dossier didn't include — caught at apply time when `write` reported "0 skills" and started to blank VERSIONS.md.
- Synthesis: route 1 (decouple + `discover_skills_root` probe, conf 9) primary; route 4 (single probe helper, conf 8) folded in by replicating the same helper in both scripts. Routes 2 (stopgap, conf 6) and 3 (CLI rename, conf 7) rejected/deferred — route 1 makes them unnecessary.
- Bugs (all conf 9-10, reproduced directly): `iter_skill_dirs(repo)` top-level only → 0 skills; exclude on `--repo` not git-root → self-mention inflation; routine-prompt stale `scripts/` path; `default_repo()` value conflated downstream. Plus the apply-time discovery: `skill_registry.py` `iter_skills`/`cmd_write`/`cmd_check` share the identical defect and `cmd_write` writes `repo/VERSIONS.md` with un-prefixed `{name}/SKILL.md` links → would corrupt links even once it found skills.
- Peer review: conf 8/10 — weakest link was the stopgap route; primary shipped.
- Decision: APPLIED — both scripts fixed, VERSIONS.md regenerated correctly (3-line diff), check now inspects real skills.

### cleanup  (v1.18.0, FORK, mentions 140, days-since 1.7) — the only frictionful skill
- STORM scan: friction is a systemic docs-mode gap (~540 orphan vault-logs + ~1299 broken wikilinks), framed in-session as a CLASS decision, not per-file misses.
- Contradiction map: agreed = surface-only, do NOT auto-write the fork; diagnose INDEX curated-vs-generated and bucket the broken links first · blind-spot = "regenerate from directory" may be the wrong primitive entirely if INDEX.md is curated (would destroy ordering/descriptions); the 1299 broken links likely stem from a prior mass-rename — fix the rename, not the symptom.
- Synthesis (ranked): (1) read-only diagnostic of INDEX.md contract [conf 9]; (2) bucket broken wikilinks by failure mode [conf 8]; (3) only then a gated detect→decide→confirm `docs index-decide` card [conf 8]; (4) port decision-logic not write-code into the fork [conf 9].
- Peer review: conf 7/10 — the regen button is a guess until INDEX.md is read.
- Decision: DEFERRED — fork-aware (§R4.2) + no autonomous high-blast-radius write on a fork. Left as a proposal; next run should run the read-only diagnostic first.

### new-skill candidates
- `corpus_integrity_guard` [conf 7] — post-scan check that warns when a discovered corpus root overlaps the skills root (would have auto-flagged the supabase=481 self-inflation). DEFERRED: route-1 exclude-fix already removes the inflation; revisit if a second consumer appears.
- `wikilink-triage` [conf 8] / `index-audit` [conf 7] — fall out of the cleanup deferral; reconsider only if cleanup docs-mode work is greenlit. DEFERRED.

---

# routine 2026-06-26 — Run 2 (second daily pass)

## Run Summary (Run 2)
- Applied: `transactions-domain-model` rewrite → v1.3.0 [conf 9/10] — finished the 1.2.0-deferred per-name v2 rewrite in the body: §4 trigger name-lookups corrected `council_members.n_name` → v2 member identity (`users` / v2 member view, exact source column flagged for live re-check); §9 write transport corrected to `callServerRpc → /api/rpc` (2026-06-19), `requireAuth()+createAdminClient()` marked historical. Route paths unchanged.
- Applied: `performance` fix → v1.0.2 [conf 9/10] — Measurement table: replaced deprecated `TTI` row (dropped by Lighthouse v10, 2023; never a CWV) with `INP < 200ms p75` (replaced FID Mar 2024) + added missing `CLS < 0.1` (the table's own web-vitals snippet imports `onCLS`); added CWV callout, fixed §References metric list.
- Created: none.
- Deferred: `supabase` [routes conf 8-9] — VENDORED/upstream-maintained (`supabase/agent-skills`); proposed additions (framework-integration, local-dev, Supavisor pooling, branching, key model) are large and would be clobbered on re-vendor. Leave to upstream; revisit only the description↔body coverage mismatch (BUG conf 9) if we choose to diverge. `conquering-campaign` [§5.5 self-consistency, conf 9] — FORK + large body; the risk-sweep-gate contradiction is real but subtle and fork-aware editing is high-blast-radius autonomously; defer to a focused pass. `bug-fix-workflow` [bug conf 8] — "no notification fires" stated as fact; verify against live trigger set before editing. `dev-server`/`codex-law-translate` — best routes conf 6-7, below the 8/10 bar.
- Cost: MiniMax doer this pass ~ STORM 8 dossiers (6 parallel + 2 retried long-timeout) + 1 edit draft ≈ 30k tokens. Skills STORMed (Run 2): 8 (supabase, cleanup, performance, dev-server, conquering-campaign, bug-fix-workflow, codex-law-translate, transactions-domain-model). Roots: 3.
- Next-best proposal: still the stale scheduled-task definition (`~/.claude/scheduled-tasks/skillsmith-routine/SKILL.md`) hands stale paths — `skillsmith/scripts/routine_scan.py` (now `star-alliance-skills/skillsmith/scripts/...`) and `star-alliance-arsenal/minimax.py` (now `star-alliance/star-alliance-arsenal/minimax.py`). Worked around again this run. It lives OUTSIDE the star-alliance repo → cannot fix under the repo-scope rule; Atta should repoint it.

## Harvest (Run 2)
- active (31): supabase, cleanup, performance, dev-server, conquering-campaign, bug-fix-workflow, codex-law-translate, transactions-domain-model, skillsmith, article-creator, db-rename-sweep, graphify …
- frictionful (1): cleanup (same docs-mode gap as Run 1 — NO new action; Run 1 already triaged + deferred)
- dormant (1): storm-investigation
- new-skill hints: council×63, public×57, atta×50, members×48, status×43, panel×41, read×34, docs×34
- Confirms Run 1's skillsmith fix is live: scan returned 31 skills / 3 roots with de-inflated mentions (supabase 435 genuine, not self-inflated).

## Per-skill dossiers (Run 2)

### transactions-domain-model  (v1.2.0→1.3.0, own, mentions 10, days-since ~5)
- STORM scan (MiniMax doer): strongest multi-route consensus of the run — two independent conf-9 routes (§4 column refs, §9 transport) plus conf-8/9 supporting bugs. The skill's own 1.2.0 changelog already DEFERRED exactly this ("full per-name rewrite needs live DB read"); the top banner already states the v2 facts, so the body was self-contradictory.
- Contradiction map: agreed = body must match the banner the skill already carries · blind-spot = exact v2 source column for the `*_n_name` fields not live-verified — handled by keeping the output-column names and flagging the source for live re-check rather than inventing a column.
- Synthesis: applied routes 1 (§4, conf 9) + 2 (§9, conf 9) + version bump (conf 8); deferred the column-shape-drift subsection (conf 7) and Model-A/Model-C reconciliation (conf 6) as needing live DB reads.
- Peer review: conf 7/10 — weakest link is the unverified exact source column; mitigated by the "verify live" caveat.
- Decision: APPLIED — own skill, facts corroborated by project memory (council_members→users; ALL writes via callServerRpc→/api/rpc since 2026-06-19); reviewer tightened MiniMax's 5×-repeated caveat into one source-note line.

### performance  (v1.0.1→1.0.2, vendored, mentions 91, days-since ~6)
- STORM scan (MiniMax doer): conf-9 route + conf-9 bug, both on the same stale metrics table (TTI listed as a CWV proxy).
- Contradiction map: agreed = TTI is deprecated and was never a Core Web Vital · blind-spot = the table also omitted CLS despite the testing snippet importing `onCLS` — folded into the fix.
- Synthesis: applied the TTI→INP swap (conf 9) + CLS add + CWV callout + §References fix. Deferred the larger additive proposals (bfcache, Long Tasks/PerformanceObserver, User Timing, Speculation Rules — conf 5-7) as scope-expansion on a vendored skill.
- Peer review: conf 8/10 — weakest link was breadth of additions; kept the change surgical/factual.
- Decision: APPLIED — vendored, but a pure factual correction (INP became a CWV 2026-... i.e. Mar 2024; TTI dropped Lighthouse v10) is exactly skillsmith's mandate; divergence noted in changelog.

### supabase / conquering-campaign / bug-fix-workflow / dev-server / codex-law-translate / cleanup
- supabase: DEFERRED (vendored upstream; large additions clobbered on re-vendor).
- conquering-campaign: DEFERRED (fork + subtle §5.5 self-consistency; no autonomous high-blast-radius fork edit).
- bug-fix-workflow: DEFERRED (best routes 5-7; the conf-8 "no notification fires" bug is a fact-to-verify, not a safe blind edit).
- dev-server / codex-law-translate: NO-OP (best routes 6-7, below 8/10).
- cleanup: NO-OP this pass — Run 1 already STORMed + deferred the docs-mode gap; nothing new.

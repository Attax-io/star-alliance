# routine 2026-06-29

## Run Summary
- Applied: skillsmith — Stage B dossier no-truncation guard → v1.7.1 [conf 9/10] (commit pending)
- Created: none
- Deferred: performance (INP-techniques + lazy/Suspense + SW-versioning) [conf 8-10/10] — 3-day cooldown §R4.5, and the two "c9 bugs" are incomplete-pattern/polish, not live throws; revisit after 2026-06-29 cooldown; · codex-law-translate orphan-translations [conf 8/10] — unconfirmed: only occurs under FK SET NULL (unlikely); realistic FK is CASCADE/RESTRICT (clean or loud-fail); flag human to verify FK delete-rule · codex rollback-procedure doc [conf 8/10] — hinges on same unverified cascade · cleanup docs-INDEX [conf 8] — attended-only carry (foreign repo + fork, per playbook §B standing class) · prob-stats renumber [conf 9] — MISREAD (files on disk are 00-15 in order; glossary-last is intentional) · supabase/skillsmith truncation "bugs" — phantom (caused by this run's dossier cut → the fix applied above)
- Cost: MiniMax doer, ~10 dispatches · skills STORMed: 8 · roots: 3
- Next-best proposal: after performance cooldown lifts, add an INP optimization-techniques subsection (scheduler.yield, long-task breaking, web workers) — the one genuine content gap, conf 10/10.

## Harvest
- active (53): supabase, design-tokens, cleanup, performance, dev-server, conquering-campaign, codex-law-translate, relationship-intel, skillsmith, bug-fix-workflow …
- frictionful (2): cleanup (docs-INDEX, attended-only), probability-statistics (missing gallery art tile)
- dormant (41): skipped
- new-skill hints: mostly generic corpus noise (public/have/council/will/panel) — no real gap signal this run

## Per-skill dossiers
### skillsmith  (v1.7.0→1.7.1, large, mentions 16, dsc 0.8)
- STORM scan: top "bug" = SKILL.md truncated mid-sentence — **false positive** caused by this run capping the dossier body at 9000 chars (skillsmith SKILL.md > 9000).
- Synthesis: real meta-finding — the dossier builder's hard truncation poisons STORM. Same phantom hit supabase + conquering-campaign.
- Peer review: conf 9/10 — evidence is this run itself (3 phantom truncation findings).
- Decision: APPLIED — Stage B "Dossier build guard" added to routine-playbook.md + changelog/version bump. Self-edit applied last, registry check passed 0 hard violations (§R6).

### performance  (v1.0.3, lean, mentions 83, dsc 1.8)
- STORM scan: INP optimization-techniques gap (c10); React.lazy() shown without <Suspense> (c9); service-worker caches opaque/no-versioning (c9).
- Synthesis: INP measurement already present (added by this routine 2026-06-26 v1.0.2); only the optimization-techniques subsection is genuinely missing.
- Peer review: conf 7/10 weakest link = INP gap.
- Decision: DEFERRED — skill upgraded twice by this routine 2026-06-26 (v1.0.2+1.0.3) → 3-day cooldown §R4.5. Verified the two c9 "bugs": the lazy() fragment never renders and cache.put() of opaque doesn't throw → incomplete-pattern/polish, not correctness. No cooldown override. Revisit after cooldown.

### codex-law-translate  (v1.1.0, lean, mentions 20, dsc 1.8)
- STORM scan: insert_articles.js DELETEs+reinserts law_articles (new uuids) but doesn't clear law_article_translations (c8 orphan); add rollback/clean-partial-load procedure (c8).
- Synthesis (verified in-repo): translations link by surrogate law_article_id; orphan outcome requires FK SET NULL — realistic FK is CASCADE (clean) or RESTRICT (loud fail on re-run). Bug overstated.
- Peer review: conf 7/10.
- Decision: DEFERRED — both routes hinge on the unverified law_article_translations→law_articles FK delete-rule. Human flag: confirm the FK rule; if SET NULL, add a translations-clear to insert_articles.js and a documented rollback.

### cleanup  (v1.20.0 FORK, large, mentions 133, friction 3)
- STORM scan: docs-mode INDEX.md drift (~540 orphan vault-logs + ~1299 broken wikilinks); reindex/wikilink-repair submodes; repo↔device fork-drift check.
- Decision: DEFERRED (attended-only standing class per playbook §B) — foreign repo (Lex Council), fork skill (edit-with-care §R4.2), gated on human reading the INDEX curated-vs-generated contract. One-line carry; not re-STORMed beyond confirming no new in-scope signal.

### probability-statistics  (v1.1.0, lean, mentions 2, friction 1)
- STORM scan: renumber references (claimed 12-glossary out of order, c9); missing skill-art/probability-statistics.png tile (c7).
- Synthesis (verified): files on disk ARE 00-15 in numeric order; glossary listed last is intentional (cheat-sheet/index; #12 by birth-order, 13-15 appended v1.1.0). Renumber = MISREAD. PNG = gallery/build asset, not SKILL.md content, below threshold.
- Decision: NO-OP — no real defect; PNG tile deferred (art-asset, can't generate autonomously).

### supabase  (v0.1.3, lean, mentions 416, dsc 0.8)
- STORM scan: top "bug" = SKILL.md truncated mid-sentence (c8) — **phantom**, dossier-cut artifact (this run); other routes (cross-ref dedup, security-checklist audit) c6-7.
- Decision: DEFERRED — top finding invalid; remainder below 8 and speculative on partial content. Fixed the root cause (skillsmith guard above), not the symptom.

### design-tokens  (v1.0.0, lean, mentions 288, dsc 1.4)
- STORM scan: top route = "telemetry anomaly 288 mentions/0 friction" (scan artifact, not a skill bug); soften absolute rules c7; verify reference files exist c7.
- Decision: NO-OP — nothing concrete ≥8 verifiable; the anomaly is scan telemetry, not skill content.

### conquering-campaign  (v3.9.0 FORK, large, mentions 24, dsc 0.7)
- STORM scan: references-availability gate (c9); G0-override schema tightening (c8); plus phantom "SKILL.md truncated" (c9, dossier-cut artifact).
- Decision: DEFERRED — fork (§R4.2 edit-with-care), top bug is the truncation artifact, and the design-change routes (G0 override schema, fail-open hook) need human review before touching a fork's safety boundary.

## Process note
This run's single applied fix came FROM the run: capping dossier SKILL.md bodies at 9000 chars made STORM hallucinate "file truncated" top-bugs on the 3 skills whose bodies exceed it. The Stage B guard (v1.7.1) stops the next run from re-introducing it. Net: 1 real, in-scope, evidence-backed self-improvement; everything else honestly deferred rather than force-shipped.

# Retired artifacts

guild-sync skill retired 2026-07-02 — the MCP now serves skills directly from the
repo, so on-device parity (guild-sync/device_sync) is obsolete. These files are
moved here (the mount blocks true deletion); safe to delete from the host.

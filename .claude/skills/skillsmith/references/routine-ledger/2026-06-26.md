# routine 2026-06-26

## Run Summary
- Applied: skillsmith fix → v1.1.9 [conf 9/10] — `routine_scan.py` **and** `skill_registry.py` both conflated the skills-discovery root with the git/anchor root, so after the repo restructure (skills moved under `star-alliance-skills/`) the default invocation found **0 skills**; `skill_registry.py write` would have **blanked the VERSIONS.md table**, and the `--repo` workaround leaked the repo as a corpus root (mention self-inflation). Added a shared `discover_skills_root(anchor)` probe to both scripts + anchor-relative VERSIONS.md link prefix. Verified: scan 31 skills / 3 roots (was 4) with de-inflated mentions; registry 31 skills, links keep `star-alliance-skills/` prefix.
- Created: none.
- Deferred: cleanup docs-mode INDEX.md gap [conf 8/10] — surface-only; fork guardrail + "regenerate may be the wrong primitive until INDEX.md is read." Diagnose curated-vs-generated + bucket the 1299 broken wikilinks before any wholesale regen.
- Cost: MiniMax doer ~16.7k tokens (smoke 246 + STORM 5275+3945 + draft 1973 + this). Skills STORMed: 2 (skillsmith, cleanup). Roots: 3.
- Next-best proposal: full device-config sweep for stale `claude-skills` pointers (carried from 2026-06-24) — the **scheduled-task definition** (`~/.claude/scheduled-tasks/skillsmith-routine/SKILL.md`) still hands the run the stale `skillsmith/scripts/routine_scan.py` path (worked around this run, not yet repointed to `star-alliance-skills/...`).

## Harvest
- active (31): supabase, cleanup, performance, dev-server, conquering-campaign, bug-fix-workflow, codex-law-translate, transactions-domain-model, skillsmith, article-creator, db-rename-sweep, graphify …
- frictionful (1): cleanup
- dormant (0): —
- new-skill hints: council×63, public×57, atta×50, members×47, status×43, panel×41, read×34, docs×34
- NOTE: this harvest itself surfaced the Stage-A bug below — the first scan returned **0 skills** at the default repo path; recovered by overriding `--repo`, which is what exposed the self-inflation side-effect.

## Per-skill dossiers

### skillsmith  (v1.1.8→1.1.9, lean, mentions 51→de-inflated, days-since 0.0)
- STORM scan (MiniMax doer): the bug is not the value `default_repo()` returns (git root, a correct *anchor*) but the lack of **separation downstream** — one value consumed as both skills-discovery root and corpus-exclude anchor.
- Contradiction map: agreed = decouple `anchor_root` (stable, git) from `skills_root` (layout-discovered) behind one probe helper · blind-spot = the same conflation exists in a sibling script (`skill_registry.py`) the dossier didn't include — caught at apply time when `write` reported "0 skills" and started to blank VERSIONS.md.
- Synthesis: route 1 (decouple + `discover_skills_root` probe, conf 9) primary; route 4 (single probe helper, conf 8) folded in by replicating the same helper in both scripts. Routes 2 (stopgap, conf 6) and 3 (CLI rename, conf 7) rejected/deferred — route 1 makes them unnecessary.
- Bugs (all conf 9-10, reproduced directly): `iter_skill_dirs(repo)` top-level only → 0 skills; exclude on `--repo` not git-root → self-mention inflation; routine-prompt stale `scripts/` path; `default_repo()` value conflated downstream. Plus the apply-time discovery: `skill_registry.py` `iter_skills`/`cmd_write`/`cmd_check` share the identical defect and `cmd_write` writes `repo/VERSIONS.md` with un-prefixed `{name}/SKILL.md` links → would corrupt links even once it found skills.
- Peer review: conf 8/10 — weakest link was the stopgap route; primary shipped.
- Decision: APPLIED — both scripts fixed, VERSIONS.md regenerated correctly (3-line diff), check now inspects real skills.

### cleanup  (v1.18.0, FORK, mentions 140, days-since 1.7) — the only frictionful skill
- STORM scan: friction is a systemic docs-mode gap (~540 orphan vault-logs + ~1299 broken wikilinks), framed in-session as a CLASS decision, not per-file misses.
- Contradiction map: agreed = surface-only, do NOT auto-write the fork; diagnose INDEX curated-vs-generated and bucket the broken links first · blind-spot = "regenerate from directory" may be the wrong primitive entirely if INDEX.md is curated (would destroy ordering/descriptions); the 1299 broken links likely stem from a prior mass-rename — fix the rename, not the symptom.
- Synthesis (ranked): (1) read-only diagnostic of INDEX.md contract [conf 9]; (2) bucket broken wikilinks by failure mode [conf 8]; (3) only then a gated detect→decide→confirm `docs index-decide` card [conf 8]; (4) port decision-logic not write-code into the fork [conf 9].
- Peer review: conf 7/10 — the regen button is a guess until INDEX.md is read.
- Decision: DEFERRED — fork-aware (§R4.2) + no autonomous high-blast-radius write on a fork. Left as a proposal; next run should run the read-only diagnostic first.

### new-skill candidates
- `corpus_integrity_guard` [conf 7] — post-scan check that warns when a discovered corpus root overlaps the skills root (would have auto-flagged the supabase=481 self-inflation). DEFERRED: route-1 exclude-fix already removes the inflation; revisit if a second consumer appears.
- `wikilink-triage` [conf 8] / `index-audit` [conf 7] — fall out of the cleanup deferral; reconsider only if cleanup docs-mode work is greenlit. DEFERRED.

---

# routine 2026-06-26 — Run 2 (second daily pass)

## Run Summary (Run 2)
- Applied: `transactions-domain-model` rewrite → v1.3.0 [conf 9/10] — finished the 1.2.0-deferred per-name v2 rewrite in the body: §4 trigger name-lookups corrected `council_members.n_name` → v2 member identity (`users` / v2 member view, exact source column flagged for live re-check); §9 write transport corrected to `callServerRpc → /api/rpc` (2026-06-19), `requireAuth()+createAdminClient()` marked historical. Route paths unchanged.
- Applied: `performance` fix → v1.0.2 [conf 9/10] — Measurement table: replaced deprecated `TTI` row (dropped by Lighthouse v10, 2023; never a CWV) with `INP < 200ms p75` (replaced FID Mar 2024) + added missing `CLS < 0.1` (the table's own web-vitals snippet imports `onCLS`); added CWV callout, fixed §References metric list.
- Created: none.
- Deferred: `supabase` [routes conf 8-9] — VENDORED/upstream-maintained (`supabase/agent-skills`); proposed additions (framework-integration, local-dev, Supavisor pooling, branching, key model) are large and would be clobbered on re-vendor. Leave to upstream; revisit only the description↔body coverage mismatch (BUG conf 9) if we choose to diverge. `conquering-campaign` [§5.5 self-consistency, conf 9] — FORK + large body; the risk-sweep-gate contradiction is real but subtle and fork-aware editing is high-blast-radius autonomously; defer to a focused pass. `bug-fix-workflow` [bug conf 8] — "no notification fires" stated as fact; verify against live trigger set before editing. `dev-server`/`codex-law-translate` — best routes conf 6-7, below the 8/10 bar.
- Cost: MiniMax doer this pass ~ STORM 8 dossiers (6 parallel + 2 retried long-timeout) + 1 edit draft ≈ 30k tokens. Skills STORMed (Run 2): 8 (supabase, cleanup, performance, dev-server, conquering-campaign, bug-fix-workflow, codex-law-translate, transactions-domain-model). Roots: 3.
- Next-best proposal: still the stale scheduled-task definition (`~/.claude/scheduled-tasks/skillsmith-routine/SKILL.md`) hands stale paths — `skillsmith/scripts/routine_scan.py` (now `star-alliance-skills/skillsmith/scripts/...`) and `star-alliance-arsenal/minimax.py` (now `star-alliance/star-alliance-arsenal/minimax.py`). Worked around again this run. It lives OUTSIDE the star-alliance repo → cannot fix under the repo-scope rule; Atta should repoint it.

## Harvest (Run 2)
- active (31): supabase, cleanup, performance, dev-server, conquering-campaign, bug-fix-workflow, codex-law-translate, transactions-domain-model, skillsmith, article-creator, db-rename-sweep, graphify …
- frictionful (1): cleanup (same docs-mode gap as Run 1 — NO new action; Run 1 already triaged + deferred)
- dormant (1): storm-investigation
- new-skill hints: council×63, public×57, atta×50, members×48, status×43, panel×41, read×34, docs×34
- Confirms Run 1's skillsmith fix is live: scan returned 31 skills / 3 roots with de-inflated mentions (supabase 435 genuine, not self-inflated).

## Per-skill dossiers (Run 2)

### transactions-domain-model  (v1.2.0→1.3.0, own, mentions 10, days-since ~5)
- STORM scan (MiniMax doer): strongest multi-route consensus of the run — two independent conf-9 routes (§4 column refs, §9 transport) plus conf-8/9 supporting bugs. The skill's own 1.2.0 changelog already DEFERRED exactly this ("full per-name rewrite needs live DB read"); the top banner already states the v2 facts, so the body was self-contradictory.
- Contradiction map: agreed = body must match the banner the skill already carries · blind-spot = exact v2 source column for the `*_n_name` fields not live-verified — handled by keeping the output-column names and flagging the source for live re-check rather than inventing a column.
- Synthesis: applied routes 1 (§4, conf 9) + 2 (§9, conf 9) + version bump (conf 8); deferred the column-shape-drift subsection (conf 7) and Model-A/Model-C reconciliation (conf 6) as needing live DB reads.
- Peer review: conf 7/10 — weakest link is the unverified exact source column; mitigated by the "verify live" caveat.
- Decision: APPLIED — own skill, facts corroborated by project memory (council_members→users; ALL writes via callServerRpc→/api/rpc since 2026-06-19); reviewer tightened MiniMax's 5×-repeated caveat into one source-note line.

### performance  (v1.0.1→1.0.2, vendored, mentions 91, days-since ~6)
- STORM scan (MiniMax doer): conf-9 route + conf-9 bug, both on the same stale metrics table (TTI listed as a CWV proxy).
- Contradiction map: agreed = TTI is deprecated and was never a Core Web Vital · blind-spot = the table also omitted CLS despite the testing snippet importing `onCLS` — folded into the fix.
- Synthesis: applied the TTI→INP swap (conf 9) + CLS add + CWV callout + §References fix. Deferred the larger additive proposals (bfcache, Long Tasks/PerformanceObserver, User Timing, Speculation Rules — conf 5-7) as scope-expansion on a vendored skill.
- Peer review: conf 8/10 — weakest link was breadth of additions; kept the change surgical/factual.
- Decision: APPLIED — vendored, but a pure factual correction (INP became a CWV 2026-... i.e. Mar 2024; TTI dropped Lighthouse v10) is exactly skillsmith's mandate; divergence noted in changelog.

### supabase / conquering-campaign / bug-fix-workflow / dev-server / codex-law-translate / cleanup
- supabase: DEFERRED (vendored upstream; large additions clobbered on re-vendor).
- conquering-campaign: DEFERRED (fork + subtle §5.5 self-consistency; no autonomous high-blast-radius fork edit).
- bug-fix-workflow: DEFERRED (best routes 5-7; the conf-8 "no notification fires" bug is a fact-to-verify, not a safe blind edit).
- dev-server / codex-law-translate: NO-OP (best routes 6-7, below 8/10).
- cleanup: NO-OP this pass — Run 1 already STORMed + deferred the docs-mode gap; nothing new.

---

## Run 3 (re-STORM, no-op)

**Summary:** 8-skill independent re-STORM via MiniMax doer reproduced Run 1/2 verdicts verbatim — zero deltas, no apply.

- **supabase** → top route: hold / no change (conf 9)
- **cleanup** → top route: docs-mode wholesale INDEX.md pass (route conf 8); **§R4.1 gate fail** (peer_review.conf 7, fork → hand-sync); deferred, consistent with Run 1
- **performance** → top route: no change (conf 9); under §R4.5 cooldown (Run 1/2 shipped)
- **dev-server** → top route: retain / keep as-is (conf 8)
- **conquering-campaign** → top route: hold v3.8.3 / no change (conf 7)
- **bug-fix-workflow** → top route: hold / keep as-is (conf 9)
- **codex-law-translate** → top route: hold / no change (conf 9); BUG>=8 closed as known limitation
- **transactions-domain-model** → top route: no change (conf 9); under §R4.5 cooldown (Run 1/2 shipped); BUG>=8 closed as known limitation

**Cooldown note:** skillsmith, performance, transactions-domain-model locked under §R4.5 (3-day) — not re-evaluable today. Gap signals (council/public/atta/members/status/panel) = Lex-Council domain noise; MiniMax rated new-skill ideas 2-3/10 → none pursued.

**Next-best proposal (diagnostic, not applied):** cleanup docs-mode wholesale INDEX.md pass — re-queue after §R4.1 peer-review upgrade to conf >=8 or after the fork hand-sync window opens.

**Doer:** MiniMax M3 (smoke 242 + 8 STORM dossiers + this draft). Skills STORMed: 8. Roots: 3. Applied: 0. Created: 0. Deferred: 1 (cleanup docs-INDEX).

---

## Run 4 (verification, no-op)

- **Date / pass:** 2026-06-26, 4th autonomous skillsmith pass
- **Scan:** `routine_scan.py` byte-identical to Run 3 (~50 min prior). 31 active, 1 frictionful (cleanup), 1 dormant (storm-investigation). No new mention/friction signal.
- **Gap signals:** unchanged — council/public/atta/members/status/panel = Lex-Council domain noise.
- **>=8/10 routes:** all settled. APPLIED today: skillsmith 1.1.9, transactions-domain-model 1.3.0, performance 1.0.2. DEFERRED (correctly): cleanup docs-mode (fork + high-blast-radius, needs read-only INDEX.md diagnostic first), conquering-campaign (fork), bug-fix-workflow (fact-to-verify). skillsmith / performance / transactions-domain-model under R4.5 3-day cooldown.
- **STORM decision:** **NO** full re-grind. Run 3 already re-STORMed all 8 and confirmed verbatim; a 4th pass would burn ~30k MiniMax tokens to reconfirm identical verdicts.
- **Actions:** applied 0, created 0.
- **External blocker (out of repo-scope):** `~/.claude/scheduled-tasks/skillsmith-routine/SKILL.md` still hands stale paths (`skillsmith/scripts/...` + old MiniMax path). Lives outside star-alliance; **Atta must repoint.** Flagged for follow-up, not actionable here.

---

## Run 5 (verification, no-op)

- **Date / pass:** 2026-06-26, 5th autonomous skillsmith pass.
- **Scan:** steady-state — 31 active, 1 frictionful (cleanup, same docs-mode INDEX gap as Runs 1-4, identical snippets), 1 dormant (storm-investigation). `request_lines_seen` 454; gap signals unchanged (council/public/atta/members/status/panel = Lex-Council domain noise). Newest corpus transcript (09:10) is **this routine's own session** — no new real-work sessions since Run 4 (08:12), so zero new mention/friction signal.
- **>=8/10 routes:** all settled. APPLIED earlier today: skillsmith 1.1.9 (Run 1), transactions-domain-model 1.3.0 + performance 1.0.2 (Run 2). DEFERRED (correctly): cleanup docs-mode (fork + high-blast-radius; peer_review conf 7 < 8 gate; needs read-only INDEX.md diagnostic first), conquering-campaign (fork), bug-fix-workflow (fact-to-verify). skillsmith / performance / transactions-domain-model under R4.5 3-day cooldown — not re-evaluable today.
- **STORM decision:** **NO** re-grind. Runs 3 + 4 already re-STORMed all 8 and confirmed verbatim; corpus is byte-unchanged since. A 5th grind would burn ~30k MiniMax tokens to reconfirm identical verdicts.
- **Actions:** applied 0, created 0, fixed 0.
- **Doer:** MiniMax M3 (smoke + Run-5 summary draft, this entry). Skills STORMed: 0 (steady-state gate). Roots: 3.
- **External blocker (out of repo-scope, 5th flag):** `~/.claude/scheduled-tasks/skillsmith-routine/SKILL.md` still hands stale paths (`skillsmith/scripts/routine_scan.py` → now `star-alliance-skills/skillsmith/scripts/...`; old `star-alliance-arsenal/minimax.py` anchor). Worked around again this run. Lives outside star-alliance → cannot fix under repo-scope rule; **Atta must repoint.**

---

## Run 6 (FULL STORM — caught 3 residual correctness bugs the no-op runs missed)

### Run Summary
- **Applied (3):**
  1. `performance` 1.0.2 → **1.0.3** [conf 9/10] — two self-contradicting code examples. (a) Service-worker cache-first `fetch` handler omitted the `font` destination → woff2 fall through to network every load, silently undoing the skill's own font-preload guidance; added the `font` clause. (b) Third-party-script example labelled `Delay until interaction` but code gates on `IntersectionObserver` (visibility/scroll, not click); relabelled to `Lazy-load when visible (IntersectionObserver)` — the YouTube facade block below is the genuine interaction example.
  2. `transactions-domain-model` 1.3.0 → **1.3.1** [conf 9/10] — §2 Gate 1 STILL asserted the dead per-route transport (`POST /api/transactions/create` + `createAdminClient()`) as current fact, contradicting both the §0 banner and the §9 block that Run 2 *did* fix. Rewrote §2 to `callServerRpc → /api/rpc` (app-wide since 2026-06-19) and marked the old shape historical (rationale preserved). Also dropped the stale `council_members.` prefix on `allow_admin_conscent` (§2 Gate 2; column lives on the approval view, v2 identity = `users`).
  3. `bug-fix-workflow` 1.1.2 → **1.1.3** [conf 8/10] — Step 5 Rejected(3): `bug_reports` has no rejection-reason column (verified against the skill's own schema table), so a reason given only in chat is lost on the next pull. Now directs the operator to persist it by appending a `[Reject-reason] …` line to `br_text` (same freeform pattern the Push route uses).
- **Created:** none.
- **Deferred:** `cleanup` (FORK; docs-INDEX + leaks routes are operational dispatches on the Lex repo, not skill edits; peer-review flags the new-mode candidates may already exist in references/) · `conquering-campaign` (FORK + 71k body; conf-9 structural trims real but high-blast autonomous, defer to focused pass) · `supabase` (vendored/upstream, conf-7 hold) · `codex-law-translate` (conf-8 routes need insert_articles.js internals verification / new-script authoring — out of this run's verified scope) · `dev-server` (STORM call errored twice, below-bar prior).
- **Cost:** MiniMax doer — smoke 247 + 8 STORM dossiers (~50k, 2 batches, several HTTP-529 retries) + 2 edit-drafts (~2.5k). Skills STORMed: 8. Roots: 3. Applied: 3.
- **Convergence lesson (important):** Runs 3/4/5 were no-ops that **skipped STORM** because the scan was byte-identical, trusting that Run 1/2 had exhausted the findings. They had not — Run 2 fixed §9 of transactions but missed §2, and only touched performance's metric table, not its code examples. A real STORM this run surfaced all three residual **correctness** bugs. **Takeaway: a byte-identical scan is NOT proof the skill bodies are defect-free — it only proves the corpus (mentions/friction) is unchanged. The steady-state skip-STORM gate (§R5 budget) should not suppress a periodic full body-audit; residual bugs from a partial prior fix are invisible to the scan-hash.** All three re-touches cleared the §R4.5 3-day cooldown via its explicit "real bug (correctness), not polish" exception.
- **External blocker (6th flag, unchanged):** `~/.claude/scheduled-tasks/skillsmith-routine/SKILL.md` still hands stale paths (`star-alliance-arsenal/minimax.py` is actually `star-alliance/star-alliance-arsenal/minimax.py`; scan path is `star-alliance-skills/skillsmith/scripts/...`). Worked around again. Outside repo-scope → **Atta must repoint.**

---

## Run 7 (targeted audit of the new skill — NO-OP, corpus delta = 1 new dormant skill)

### Run Summary
- **Applied:** none.
- **Created:** none.
- **Audited (1, CLEAN):** `storm-investigation` v1.0.0 [peer-review conf 9/10] — the only corpus change since Run 6 was the **manual** addition of this new skill (commits `78513bd`, `b02f830`) + the create-playbook/skillsmith-SKILL edits that wired it. It is brand-new and had never been STORM-audited, so per Run 6's convergence lesson ("a body audit is warranted when the corpus changes, even if the scan looks quiet") it got a full STORM pass rather than a dormancy skip. **Result: no defects clearing the bar.** Structurally Cowork-clean (`skill_registry.py check storm-investigation` → 0 hard violations). Body is internally self-consistent (SKILL.md phases 1–4 ↔ prompt-pack prompts 1–4 ↔ persona-set tables all match), and it correctly disambiguates itself from both siblings (`deep-research` = web-search fan-out; `skillsmith routine` = STORM recast for skill-evolution). Investigated path refs `skillsmith/scripts/skill_registry.py write` / `skillsmith/references/storm-method.md` / `minimax.py` as a suspected stale-path bug (the class Run 1 fixed) — **NOT a bug**: they match the repo-wide shorthand convention used identically by `upgrade-playbook.md:42`, skillsmith `SKILL.md:63`, `sync-playbook.md:56`, and `skill_registry.py:220`'s own emitted hint. Only soft flag (peer-review, below bar): the "~25% more organized / ~10% broader" Stanford metrics are uncited paraphrases — verify against the NAACL 2024 paper before any future edit; not a confident defect.
- **Deferred (unchanged):** `cleanup` (FORK; docs-INDEX/leaks gap — same as Runs 1/6, no new signal, no autonomous high-blast write on a fork) · all 8 Run-6 STORMed skills remain on the §R4.5 cooldown (touched today) with no new corpus signal to override it.
- **Cost:** scan + 1 registry check + 1 attempted MiniMax STORM dispatch that **timed out** (`TimeoutError: read operation timed out` — API slow/HTTP-529-class). Per the doer architecture ("if the script is unreachable, fall back to doing the step yourself and note it; never block") the STORM audit of storm-investigation was done by Claude as fallback doer. MiniMax smoke had passed (224 tokens, `SMOKE_OK`) before the larger dispatch timed out. Skills STORMed: 1 (storm-investigation, Claude-fallback). Roots: 3. Applied: 0.
- **Discipline note:** this is the **7th** invocation today. Runs 1–2 applied the high-value fixes; Run 6 caught 3 residual correctness bugs via full STORM; Runs 3/4/5 were no-ops. The scheduler is clearly firing repeatedly. Run 7 correctly did the ONE useful thing a 7th same-day pass can do — audit the single artifact that changed since the last substantive run — and found it clean, rather than re-grinding the converged set.
- **External blocker (7th flag, unchanged):** the scheduled-task definition `~/.claude/scheduled-tasks/skillsmith-routine/SKILL.md` still hands stale `skillsmith/scripts/routine_scan.py` + `star-alliance-arsenal/minimax.py` paths (worked around again) AND is plainly over-firing (7 runs/day). Outside repo-scope → **Atta must repoint the paths and check the cron cadence (expected `0 6 * * *`, daily).**

---

## Run 8 (verification no-op — cleanup re-STORMed, conf 9/10 NO-EDIT)

### Run Summary
- **Applied:** none. **Created:** none. **Fixed:** none.
- **Why no-op:** 8th invocation today. Scan (`routine_scan.py --days 14`, 32 skills, 3 roots) shows 31 active, **1 frictionful (cleanup, same 4 docs/leaks usage snippets as Runs 1–7)**, 1 dormant (storm-investigation). No skill SKILL.md or `references/` changed since Run 7 — the only two commits since (`570e2ea` dashboard overhaul, `14b0e07` role pip) touch art/dashboard/data only. The single new real-work session post-Run-7 (`4563e8dd`, 11:23, the dashboard build) produced **zero new skill-friction**. All 8 active skills were FULL-STORMed in Run 6 (3 correctness fixes shipped) and bodies are unchanged since; storm-investigation was audited clean in Run 7. Corpus is genuinely converged.
- **STORM run this pass (1 skill, MiniMax doer):** `cleanup` (the only frictionful skill) — fresh 5-persona STORM. **Verdict: NO skill-body edit, peer-review conf 9/10.** All 5 personas converge. The friction's requested ACTION ("decide the INDEX wholesale, don't add logs one-by-one") is **already designed into the skill** — docs-mode Step D0 watermark (`scripts/watermark.py`, shipped v1.17.0) escalates to a `/conquering-campaign` AUDIT on ≥3 vault-logs since last pass (EXIT 10). I independently verified D0 exists by reading `references/mode-docs.md`. All 4 friction hits classify as operational-usage signal, not defects (snippet #2 is leaks-mode *succeeding* at detect+surface; #3–4 are the routine reporting its own output). The candidate-edit audit rejected every proposed change as below-8 (lower D0 threshold = no miscalibration evidence; force-escalate = anti-pattern that defeats the gate; doc-only repeat = adds no capability). Editing a stable FORK with no defect = net-negative drift risk.
- **Deferred (unchanged):** the **operational** docs-INDEX wholesale dispatch on the Lex repo (~540 vault-logs missing from INDEX.md, ~1299 broken wikilinks) + i18n key population surfaced by leaks mode — both are downstream operational actions, NOT skill-body edits; correct locus is a D0-triggered `/conquering-campaign` AUDIT with FORK hand-sync. `conquering-campaign` (FORK, 71k body, conf-9 structural trim still high-blast). All Run-6 STORMed skills remain under §R4.5 3-day cooldown (touched today) with no new corpus signal to override.
- **New-skill candidates:** none. Gap signals (council 63 / public 57 / atta 50 / members 48 / status 43 / panel 41) = Lex-Council domain nouns, not skill gaps — rated 2–3/10 across prior runs, unchanged.
- **Doer:** MiniMax M3 — smoke (222 tok, `SMOKE_OK`) + 1 STORM dossier on cleanup (4944 tok, conf 9 no-edit). Skills STORMed: 1. Roots: 3. Applied: 0.
- **Discipline note:** Run 6's convergence lesson (a byte-identical scan ≠ proof bodies are defect-free) was honored — rather than a blind skip, this run did the one useful thing an 8th same-day pass can do: re-STORM the single still-frictionful skill against its actual body, formally close the 6×-deferred docs route at conf 9, and confirm no body changed since the last full audit. No re-grind of the converged 8-skill set (would reconfirm Run 6 verdicts on identical post-fix bodies).
- **Scheduler blocker (8th flag, unchanged + worse):** `~/.claude/scheduled-tasks/skillsmith-routine/SKILL.md` STILL hands **stale paths** — `skillsmith/scripts/routine_scan.py` is now `star-alliance-skills/skillsmith/scripts/...` and `star-alliance-arsenal/minimax.py` is `star-alliance/star-alliance-arsenal/minimax.py` (the repo was restructured: skills moved under `star-alliance-skills/`). Self-healed again this run. AND the task is plainly **over-firing (8 runs/day vs expected `0 6 * * *` daily)**. Both live outside the star-alliance repo → cannot fix under repo-scope rule. **Atta must (a) repoint the paths in the scheduled-task SKILL.md and (b) check the cron cadence.**

---

## Run 9 (FULL STORM of the one changed skill — caught a residual 1.2.0 partial-bump)

### Run Summary
- **Applied:** `guild-log` 1.2.0 → **1.2.1** [conf 9/10] — the only corpus delta since Run 8 was `guild-log` getting **version-bumped 1.1.0→1.2.0 by a concurrent dashboard/decision-logging session** (`1aaefa0`), and that bump was a **partial rollout** (exactly the failure class Run 6 flagged): 1.2.0 shipped two new log types (`workflow`, `decision`) in `log_event.py::VALID_TYPES` (line 42) + `build.py::VERSION_MINOR_TYPES` (line 80) and a "Log decisions" section, but the SKILL.md was left half-documented — (a) the valid-`--type` table **omitted `workflow`** entirely (a session following the skill verbatim could not learn the type exists) and (b) the **changelog had no 1.2.0 row at all** (newest row was 1.1.0 while `metadata.version` already read 1.2.0). Fixed both: added the `workflow` table row + backfilled the missing **1.2.0** changelog row, and stamped a **1.2.1** row recording the consistency fix. Cowork check clean (0 hard violations); VERSIONS.md regenerated (surgical 1-row diff, anchor-relative links intact — Run-1 fix holding). guild-log is repo-only (not device-installed) → no device sync needed. Not a fork; not under §R4.5 cooldown (last touched by the concurrent actor, not the routine).
- **Created:** none.
- **Deferred (below the 8/10 bar):** MiniMax's STORM raised 4 lower-confidence items I reviewed and held — (3) `guild-data.json` appears in Steps 5/6/checklist but in no changelog row [conf ~6, speculative — it's the JSON mirror of `guild-data.js`, arguably an implied artifact]; (4) the description's "Ask Atta to confirm before writing" has no explicit workflow gate [conf ~6 — same line exists in `vault-log-compliance`, likely a project-wide implicit convention]; (5) Step 4's inline `<dashboard|structure|chore>` placeholder omits `decision`/`workflow` [conf ~5 — `decision` has its own dedicated section + example, so the narrow change-flow list is defensible]; (6) Pitfall #1 (hide sibling dashboard views) is a dashboard-engineering concern, not a logging one [conf ~5 — scope judgment, not a defect]. None clear §R4.1.
- **Cost:** MiniMax doer — 1 STORM dossier on guild-log (12.1k tokens; **`--json` parse failed twice + 1 SSL read-timeout**, so the dossier came back as prose on stderr). I captured the full prose STORM and **gated on my own source-verification** (read `log_event.py`, `build.py`, the changelog table directly) rather than the unparseable JSON — both applied defects are facts checked against the actual scripts, not model opinion. Skills STORMed: 1. Roots: 3. Applied: 1.
- **Next-best proposal / blockers:** same two unchanged externals — the stale scheduled-task paths and the over-firing cadence (now **9 runs/day**). Both outside repo scope; Atta must repoint + retime.

### Harvest (Run 9)
- active (31), frictionful (1: cleanup — same docs-mode signal, Run-8 closed at conf 9, NO new action), dormant (1: storm-investigation, audited clean Run 7). Gap signals unchanged (council/public/atta/members — Lex-Council domain nouns).
- **Convergence-lesson application:** Runs 3/4/5/8 correctly skip-STORMed the converged 8-skill set. This run did NOT skip: the scan surfaced a genuine skill-body delta (`guild-log` 1.2.0 bump), so per Run 6/7's rule ("a body audit is warranted when the corpus changes, even if the scan looks quiet") the changed skill got a full STORM — and it was carrying a residual partial-bump defect invisible to the scan hash. Exactly the case the lesson predicts.

---

# routine 2026-06-26 — Run 10 (tenth daily pass)

## Run Summary (Run 10)
- **Applied:** none. **Created:** none. **Fixed:** none. Clean confirmatory no-op.
- **Why no-op:** the SKILL.md corpus is **unchanged since Run 9** (`git log 71559da..HEAD` empty; no SKILL.md touched in the last 30 min). The 9 prior passes today already applied every route that cleared the §R4 bar (skillsmith→1.1.9, transactions-domain-model→1.3.0, performance→1.0.2, Run-4 batch, guild-log→1.2.1) and soundly deferred the rest. The only frictionful skill (`cleanup`, 4 snippets) is the **same docs-mode INDEX/wikilink gap** triaged + deferred in Runs 1–2 — no new friction.
- **One lead re-examined and ruled out with evidence:** the Run-2-flagged `supabase` "description↔body coverage mismatch (BUG conf 9)". Read the actual body directly: principle #1 makes the skill an **intentional documentation-router** (fetch changelog → look up topic in live docs) rather than per-product prose, so the description trigger-list (Realtime/Edge Fns/Vectors/Cron/Queues) covering products the body doesn't narrate is **by design, not a defect**. MiniMax STORM independently reached the same verdict and **reclassified the conf-9 "bug" as architectural** (DEFER, conf 9/10). Supabase is vendored upstream + 0 friction across 436 mentions → an unsolicited description edit has negative expected value (clobbered on re-vendor, no factual basis). DEFERRED, now with direct-read evidence rather than a guess.
- **Working-tree note (no action taken):** the repo tree is dirty from a **concurrent actor** (dashboard/guild-log session) — `build.py`, `log_event.py`, `guild-data.*`, 9 `star-alliance-members/*.md`, untracked `conformity_check.py` + 4 `workflow-art/*.png`. Per project rule + memory (don't auto-commit others' in-flight changes) I committed **only this ledger file**; everything else left untouched. Flag for watch: the uncommitted `build.py` + `log_event.py` edits land on guild-log right after Run 9 fixed its 1.2.0 **partial-rollout** — if that session ships another `VALID_TYPES`/`VERSION_MINOR_TYPES` change without the SKILL.md `--type` table + changelog row, it re-introduces the exact class Runs 6/9 flagged. Not mine to pre-empt; next routine pass should re-STORM guild-log once those land.
- **Cost:** MiniMax doer — smoke (`SMOKE_OK`) + 1 STORM dossier on supabase (2601 tok; `--json` parse failed → captured as prose on stderr, gated on my own direct body read). Skills STORMed: 1 (supabase). Roots: 3. Applied: 0.
- **Next-best proposal (carried, unchanged):** the scheduled-task definition at `~/.claude/scheduled-tasks/skillsmith-routine/SKILL.md` still hands stale paths (`skillsmith/scripts/routine_scan.py` → now `star-alliance-skills/...`; `star-alliance-arsenal/minimax.py` → now under `star-alliance/`). Worked around again this run. It lives OUTSIDE the star-alliance repo → cannot fix under the repo-scope rule; **Atta should repoint it.**

## Harvest (Run 10)
- active (31): supabase, cleanup, performance, dev-server, conquering-campaign, bug-fix-workflow, codex-law-translate, transactions-domain-model, skillsmith, article-creator, db-rename-sweep, graphify …
- frictionful (1): cleanup (same docs-mode gap — NO new action; triaged Runs 1–2)
- dormant (1): storm-investigation
- new-skill hints: council×63, public×57, atta×50, members×48, status×43, panel×41, read×34, docs×34 — all Lex-Council-app domain nouns, no skill-shaped gap (consistent with prior passes).

## Per-skill dossier (Run 10)

### supabase  (v0.1.2, VENDORED upstream, mentions 436, friction 0)
- STORM scan (MiniMax doer, conf 9/10 DEFER): factual auditor found no verifiable stale claim (body principle 1 forbids asserting per-product facts from training data); contradiction hunter found the prior conf-9 "mismatch" is in direct tension with the body's own router design (trigger-list ≠ prose-coverage); risk assessor — vendored + 0 friction across 436 mentions = unsolicited edit has negative EV.
- Reviewer (Claude) gate: agreed, and went further — read the actual 112-line body to confirm the router design (principle #1 + Documentation/MCP sections delegate per-product detail to live lookup). The Run-2 "BUG conf 9" is **reclassified architectural, not defective**. No surgical factual correction exists that clears conf≥8 without diverging an accurate upstream description.
- Decision: DEFERRED (evidence-backed). Reopen only on a *specific* user-reported failure (wrong URL / renamed flag in a friction log), never on a memory-sourced guess for a vendored zero-friction skill.


---

# routine 2026-06-26 — Run 11 (eleventh daily pass)

## Run Summary (Run 11)
- **Run 11 / 2026-06-26** — 11th autonomous pass; prior 10 already converged (skillsmith 1.1.9, transactions-domain-model 1.3.1, performance 1.0.2, guild-log 1.2.1 all APPLIED; docs-INDEX, supabase desc/body, conquering-campaign self-consistency all DEFERRED for documented cause).
- **STORM sweep** — re-STORMed 8 skills (supabase, cleanup, performance, dev-server, conquering-campaign, bug-fix-workflow, codex-law-translate, transactions-domain-model) via MiniMax doer. Every ≥8 finding was either (a) 9k-char dossier truncation artifact (false "section truncated" bugs from MY dossier cut, not the real files), (b) already-applied-today / cooldown (e.g. MiniMax flagged transactions "45-column zero-drift" as stale conf-8; the file already DEBUNKS that claim at lines 46/218 and changelog 1.2.0 retired it — false positive), or (c) deferred-for-cause. **0 findings cleared §R4 for fresh auto-apply.**
- **One genuine new corpus delta since Run 10** — a concurrent actor STAGED (uncommitted) a `cleanup` fork-reconciliation bumping `metadata.version` 1.18.0 → 1.20.0 with three HTML comment blocks (1.18/1.19/1.20) documenting the device-lineage i18n DB-native graft + the repo `docs` escape credit. Registry check: **0 hard violations, Cowork-clean**. BUT it is a **partial-bump defect** (same class guild-log hit Run 9): the canonical Changelog TABLE at line 278 still tops out at **1.15.0** — missing the 1.18.0 / 1.19.0 / 1.20.0 rows — violating the skill's own line-276 contract ("prepend a row to §Changelog on every bump").
- **Decision: FLAGGED, NOT FIXED** — the change is in-flight on a FORK and uncommitted. §R4.2 = no blind edits to others' in-flight work; project rule + memory = never auto-commit/edit a concurrent actor's staged changes. The actor may still complete the changelog rows before committing. Next routine pass should re-STORM `cleanup` **once the bump lands as a commit** — mirroring how Run 9 fixed guild-log only AFTER its bump committed. (Also noted: description measures right at the ≤1024 boundary; registry gate — the canonical §R4.3 check — reports clean.)
- **Carried blocker (unchanged)** — `~/.claude/scheduled-tasks/skillsmith-routine/SKILL.md` still hands stale paths; **hit at Stage A this run** (`routine_scan.py` now lives under `star-alliance-skills/skillsmith/scripts/`, not `skillsmith/scripts/`), worked around. Out of repo scope; awaits Atta repoint.
- **Cost** — MiniMax doer: smoke + 8 STORM dossiers (~50k tokens) + this summary draft. Skills STORMed: 8. Roots: 3. Applied: 0. Created: 0. Fixed: 0.

## Harvest (Run 11)
- active (31): supabase, cleanup, performance, dev-server, conquering-campaign, bug-fix-workflow, codex-law-translate, transactions-domain-model, skillsmith, article-creator, db-rename-sweep, graphify …
- frictionful (1): cleanup (same docs-mode INDEX/wikilink gap — triaged + deferred Runs 1–2/8; NO new action)
- dormant (1): storm-investigation (audited clean Run 7)
- new-skill hints: council×63, public×57, atta×50, members×48, status×43, panel×41, read×34, docs×34 — all Lex-Council domain nouns, no skill-shaped gap (consistent with every prior pass).

## Per-skill dossier (Run 11)

### cleanup  (v1.18.0→1.20.0 STAGED by concurrent actor, FORK, mentions 140, friction 4)
- STORM scan (MiniMax doer): friction unchanged (docs-mode INDEX/wikilink class, already deferred). Real signal this run came from the corpus diff, not the scan hash — per the Run 6/9 lesson, the staged body bump warranted a direct audit.
- Audit (Claude, source-verified): version + body heading both → 1.20.0 ✓; three comment blocks document 1.18/1.19/1.20 ✓; description Cowork-clean ✓. **Defect:** §Changelog TABLE (line 278) newest row = 1.15.0; the 1.18.0/1.19.0/1.20.0 rows that the skill's own line-276 rule mandates are absent — a partial rollout.
- Decision: **FLAGGED, NOT FIXED** — in-flight + fork; not the routine's to commit or edit. Re-STORM once committed.

### the other 7 (supabase, performance, dev-server, conquering-campaign, bug-fix-workflow, codex-law-translate, transactions-domain-model)
- All re-STORMed; verdicts reproduce the prior adjudications. No SKILL.md changed since Run 10 (`git diff db45142..HEAD -- '*SKILL.md'` empty). All ≥8 routes were truncation artifacts, cooled-down (verified current), or deferred-for-cause. **No-op, as Runs 8 + 10.**

---

# routine 2026-06-26 — Run 12 (twelfth daily pass)

## Run Summary (Run 12)
- **Applied:** none. **Created:** none. **Fixed:** none. **Flags closed:** 1. **Proposals logged:** 1. A verification pass with one resolution and one concrete new proposal — not a blind no-op.
- **Corpus delta since Run 11:** real this time — three commits landed (`80f38bc` cleanup fork 1.18→1.20 reconcile, the Air-pass `620a078`, the design-language reconcile `57f995d`) plus a manual `bug-fix-workflow`→1.1.4 edit. So Run 12 did NOT skip-STORM on a converged hash; it audited each changed surface directly (the Run 6/9 lesson: a body delta warrants a direct audit even when the scan looks quiet).
- **Run 11's open flag → RESOLVED (verified).** Run 11 deferred the `cleanup` 1.20.0 **partial-bump** (version bumped, §Changelog table allegedly topped at 1.15.0) because it was staged on a fork. It has since **committed** (`80f38bc`), and the committed file is **complete**: changelog rows **1.20.0 / 1.19.0 / 1.18.0** are all present (SKILL.md lines 282–284), continuous down to 1.15.0; version + body heading both 1.20.0; description Cowork-clean (989/991-char lean edition). The defect the staged copy carried was fixed before/at commit. **No action — flag cleared.** (Mirrors Run 9's guild-log: the bump's author completed the changelog at commit time.)
- **New skill audited — `design-language` (v1.0.0), CLEAN.** Landed this session (the `fallen-sword-design-language` → multi-mode voice-engine merge + reconcile). Spec audit: desc 989 chars no `<`/`>` ✓; body 98 lines ✓; §Changelog present ✓; in `skills-meta.json` ✓; assigned to `the-designer` ✓; **K-invariant holds** (dir == meta == generated id). No-op, like Run 7's storm-investigation audit.
- **One fresh REAL finding — logged as a proposal, below the auto-apply gate.** Registry check: **`conquering-campaign` body = 10,351 words, ~351 over the ~10k Cowork soft ceiling** ("WARN … lean-pass candidate"). It is genuine, but NOT auto-applied: (a) the **hard** validator gate is clean — `skill_registry.py check` prints *"No hard violations — all skills are Cowork-installable"* (the 1024-char description rule, the only validator-enforced limit, passes); (b) it is a **documented FORK** (repo = lean Cowork edition, device = rich canonical) → §R4.2 fork-aware handling, no blind trim; (c) the overage is **3.4%** of a *soft* estimate; (d) the remedy risks cutting campaign routing logic from the guild's most complex skill. **Confidence to auto-edit ≈ 6/10 — below the ≥8 bar → PROPOSAL.**
  - **Concrete proposal for a human / future run:** extract **§Common failure modes + §Anti-pattern quick-lookup** (SKILL.md lines ~379–453, ~74 lines of *descriptive* failure catalog) to `references/failure-modes.md`, leaving a `Read references/failure-modes.md` pointer. **Guard:** routing logic stays in the body — pre-flight gates, Standing principles, the Trigger-phrase precedence table, the Autonomous decision ladder, and the Step 0–6 structure do NOT move; only the descriptive failure-mode catalog does. Est. −700–1000 words → clears 10k with margin. Apply via the fork-aware `upgrade` path (edit the correct edition, re-apply lean packaging, hand-sync the device copy; never rsync monolith over stub) + PATCH bump.
- **No new-skill gap.** Harvest hints (panel×19, closure×10, admin×8, balances×7) are Lex-Council domain nouns, no skill-shaped gap — consistent with all 11 prior passes.
- **Cooldown respected.** `bug-fix-workflow` (1.1.4, this session) and the Run-6/9 batch (performance, transactions-domain-model, guild-log, skillsmith) all touched <3 days → not re-edited; none showed a fresh correctness defect.
- **Cost:** no MiniMax STORM fan-out this run. A full 8-skill re-STORM would reproduce Run 11's verdicts on a known-converged set (~50k tokens for a guaranteed no-op); per the convergence-lesson (Runs 3/4/5/8) the changed surfaces were audited directly instead. Skills audited: 2 (cleanup-flag verify, design-language). Roots: 3. Applied: 0.
- **Conformity-close:** `conformity_check.py` → **✓ FULL CONFORMITY** (read-only; `build.py` deliberately NOT run — no skill changed this pass, so rebuilding would only churn `guild-data` and risk re-entangling concurrent-actor work, the trap this session already hit once). Commit stages the ledger + scan log only, by explicit path.
- **Carried blocker (unchanged):** the scheduled-task definition at `~/.claude/scheduled-tasks/skillsmith-routine/SKILL.md` still hands stale pre-restructure paths; out of repo scope → **Atta must repoint it.** Also: the routine has now fired 12× today — cadence is far over the intended daily; **Atta should retime it.**

## Harvest (Run 12)
- 41 skills · window 14d · 3 roots. active (15), **frictionful (0)**, dormant (26, incl. the new `design-language`).
- new-skill hints: panel×19, have×10, under×10, closure×10, admin×8, button×8, balances×7 — all Lex-Council domain nouns, no skill-shaped gap.

## Per-surface audit (Run 12)

### cleanup (v1.20.0, FORK, committed `80f38bc`) — Run-11 flag RESOLVED
- Direct source-read: version 1.20.0, body heading 1.20.0, §Changelog rows 1.20.0/1.19.0/1.18.0 all present + continuous, desc Cowork-clean. The partial-bump the staged copy carried is gone. NO action.

### design-language (v1.0.0, NEW this session) — CLEAN
- Spec + K-invariant audit all green (see summary). Audited-clean no-op.

### conquering-campaign (v3.8.3, FORK) — 10,351w > ~10k soft ceiling
- Real lean-pass finding; deferred below the ≥8 gate with a concrete extraction proposal + routing-logic guard (see summary). Re-evaluate on the next deliberate `upgrade` pass.

# routine 2026-06-20

> **Five runs fired today.** Run 1 (desc-counter fix → v1.1.3), Run 2 (source-class filter → v1.1.4),
> Run 3 (worktree/catalog/self-prompt drops → v1.1.5), Run 4 (usage-vs-topic residual → v1.1.6),
> Run 5 (this top section — **the message-author provenance lever, found to be STRUCTURAL not
> lexical** → v1.1.7). All five touch only `skillsmith`, all under the §R4.5 cooldown exception (a
> machine-authored-noise friction signal is a Stage-A correctness defect, not polish). The recurring
> friction-noise item is now down to a 3-snippet structurally-untagged tail (paste/plain-text dispatch).

## Run 5 Summary
- **Applied:** `skillsmith` bug-fix → **v1.1.7** [conf 9/10] (pushed) — discovers that the
  **message-author provenance** signal runs 2–4 repeatedly deferred as "needs turn-origin semantics,
  NOT reachable by lexical filters" is in fact **STRUCTURAL**. The routine's own Agent/Workflow fan-out
  injects user-role *dispatch prompts* (e.g. a STORM `"MISSION: extract recurring HYGIENE DEBT…"`
  prompt) that log as plain user turns and survived every prior filter (`cleanup`×5). Fix in
  `routine_scan.py` `message_texts`: drop a user turn carrying ANY of three machine-origin markers,
  none of which a genuine typed turn has —
  1. **`isSidechain: True`** (+ `agentId`) — the SUBAGENT's own session; the dispatch prompt is that
     subagent's first user turn. Canonical subagent marker.
  2. **`sourceToolAssistantUUID`** — the PARENT session's record of a dispatched-tool prompt.
  3. **`toolUseResult` not null** — tool_result blobs the parent logged as user turns.
  - **Ground-truthed** on a Lex session: **6/6** genuine Atta turns carry NONE of these; **110/110**
    machine turns (incl. all 4 logged copies of the HYGIENE-DEBT dispatch prompt) carry one.
  - **Verified by before/after scan:** friction **6 → 3 snippets** (`cleanup` 5→2, `dev-server` 1→1) —
    the 3 cleanly-tagged self-dispatch prompts gone, 6/6 real turns preserved. `check skillsmith` = 0
    hard violations; VERSIONS.md written; device synced 1.1.7; `routine_scan.py` repo↔device in sync.
- **Created:** none. **STORM fan-out:** none — corpus unchanged since Run 1's morning fan-out; the only
  ≥8/10 finding was this harvest-correctness fix. Re-fanning 5×8 personas to reconfirm the active set is
  negative-ROI (consistent Economist call across runs 2–5). Skills acted on: 1 (`skillsmith`). Roots: 3.
- **Deferred (the last, hardest tail — 3 structurally-UNTAGGED survivors, ~5/10):**
  1. A pasted vault-log / `MEMORY.md` line (`dev-server`×1) — logs as a clean user turn, zero marker.
  2. A `"Task: …"`-prefixed dispatch prompt (`cleanup`×1) that landed as a plain user turn (no
     sidechain/tool keys — a Workflow `agent()` prompt recorded without provenance tags).
  3. A numbered analysis-report line (`"3) • MED — ~540 vault-logs…"`, `cleanup`×1) — a pasted/echoed
     report body, structurally a real turn.
  All three carry NO structural marker, so only **lexical dispatch/paste-format heuristics** could
  catch them (`MISSION:`/`Task:` dispatch prefixes, numbered-report shapes) — fragile, high
  over-exclusion risk against real user turns. Below the auto-apply bar; left as the standing proposal.
- **Next-best proposal:** a *narrow* lexical dispatch-prefix drop — reject a friction line whose
  message **begins** with `MISSION:` / `Task:` (the Agent/Workflow dispatch idiom), anchored at message
  start only (not mid-line) to bound over-exclusion. That clears survivors #2 (and the #3-class) without
  touching conversational turns. The paste-of-doc survivor (#1) has no reliable signal and should stay a
  permanent accepted-noise floor rather than risk dropping genuine user references.

## Run 4 Summary
- **Applied:** `skillsmith` bug-fix → **v1.1.6** [conf 9/10] (pushed) — reframes the friction signal in
  `routine_scan.py` from a *mention* signal to a *live-usage* signal, resolving the class-(B)
  topical/word-collision Runs 2 & 3 deferred as needing "a usage-vs-topic semantic gate." Two changes:
  1. **Friction is SESSION-USER-TURN-ONLY** [9/10] — project files (app `docs/`, vault-logs, memory) feed
     `mention_total` (topical interest → STORM ranking) but can no longer be friction. A skill name in a
     vault-log is a *domain topic* (cleanup the activity, performance the metric, supabase the product)
     next to ordinary software-friction words; the file *describes* work, doesn't *exercise* the skill.
     This alone dropped **32 of 36** snippets — the project-file half prior runs kept path/keyword-filtering.
     Prior runs over-thought it as "semantic, not reachable by filters" because they tried to *keep*
     app-doc friction; the clean cut is to *exclude* it (only a live session exercises a skill).
  2. **`is_skill_ref()` usage gate** [9/10] on the surviving session friction — the name must appear as a
     real skill/command reference (`/name` or "name skill/mode/command"), not a bare domain noun. The
     looser "run/use/invoke name" verb-form is rejected on purpose: for a product-named skill it collides
     hard ("**Use the Supabase** MCP" = the product; "the cleanup **runs**" = the activity). Higher
     precision / lower recall is the right trade for an autonomous gate.
  - Plus `SKILL_META` extended to drop `**/name/SKILL.md` path globs + read/locate-the-skill user lines.
  - **Verified by re-running the scan:** friction **36 → 6 snippets, frictionful 8 → 2** (`cleanup`,
    `dev-server`). The bare-domain-noun false positives — `supabase`×6 ("Supabase project_id=…"),
    `performance` ("performance lessons"), `bug-fix-workflow`, most `cleanup` — are gone. Every survivor
    now genuinely references the skill-as-skill.
- **Created:** none.
- **Deferred (low-volume edge residuals, ~5/10, both need turn-origin/author semantics):**
  1. A user **pasting a vault-log line** that literally says "dev-server skill" (1 `dev-server` snippet) —
     reads as a genuine reference; only context/authorship distinguishes paste-from-doc vs live complaint.
  2. The routine's **own prior STORM dispatch prompts** ("the cleanup skill/mode should catch X", 5
     `cleanup` snippets) — these ARE legitimate cleanup-upgrade hints, but they're machine-authored by a
     past run, not Atta's live friction. Separating them needs message-author provenance, not lexical
     filters. Neither is harmful (both name real upgrade signal); left as proposals.
- **Cost:** direct scan ground-truthing (simulate-gate → measure → tighten, 3 iterations) + 1 bundled
  PATCH to `routine_scan.py`. No STORM fan-out: the active corpus is unchanged since Run 1's pass and the
  only ≥8/10 finding was the harvest-correctness fix; re-fanning 5×8 personas to reconfirm "the other 7
  frictionful skills are noise" is negative-ROI (Economist persona). Skills acted on: 1. Roots: 3.
- **Next-best proposal:** add **message-author provenance** to `routine_scan.py` (tag each transcript line
  human-typed vs machine-dispatched vs pasted-quote) — that's the one remaining lever that would close
  both Run-4 edge residuals and is the last structural gap in the friction signal.

## Run 3 Summary
- **Applied:** `skillsmith` bug-fix → **v1.1.5** [conf 9/10] (commit `fc98f85`, pushed) — three more provable
  non-usage friction sources excluded in `routine_scan.py`, ground-truthed against this run's own scan paths:
  1. **`.claude/worktrees/` (+ all `.claude/`) project files** [9/10] — `cleanup`×3 and `supabase`×3 came
     from `…/Lex Council App/.claude/worktrees/agent-*/…` (transient agent repo checkouts = duplicate
     campaign docs). Run 2's `is_definitional` only caught `.claude/skills|commands/`; worktrees slipped
     through. Fixed by adding `.claude` + `worktrees` to `SKIP_DIRS` (sessions root `~/.claude/projects`
     is walked from *inside* `.claude`, so SKIP_DIRS pruning never touches it — verified).
  2. **available-skills catalog injection (Run 2's deferred class A)** [8/10] — `vault-log-compliance`×6
     are the literal `lex_cleanup` *description* echoed by the system skill catalog; the matched name sat
     past the 280-char snippet → misattributed. Run 2 deferred at ~7 citing marker uncertainty; the marker
     is in fact stable (`available for use with the Skill tool` / `Available agent types for the Agent tool`).
     Dropped at message level via the same mechanism as the existing `Base directory for this skill:` drop.
  3. **routine/scheduled-task self-prompt** [8/10] — `skillsmith`×3 are THIS routine's own Stage-D control
     line re-injected each run. Dropped messages containing `<scheduled-task`. Plus a per-line frontmatter
     `description:` guard (belt-and-suspenders for any description echo outside `.claude/`).
- **Created:** none.
- **Deferred:** Run 2's class (B) topical/word-collision [conf ~5/10, hard] — `supabase`/`performance`
  matching legitimate `docs/` *prose by skill-name-as-topic*. Path/keyword exclusion can't catch it; needs
  a usage-vs-topic semantic gate. Unchanged from Run 2 — stays the standing next-best proposal.
- **Cost:** direct scan ground-truthing (per-skill snippet→source-path audit) + 1 bundled PATCH to
  `routine_scan.py`. No STORM fan-out — Run 1 already STORMed the active set this morning and the corpus is
  unchanged; re-fanning would churn. Skills acted on: 1 (`skillsmith`). Roots: 3.
- **Next-best proposal:** the class-(B) semantic gate — require a friction snippet to co-occur with a
  request verb inside a *typed* `('user', str)` transcript message before it counts. It's the last
  remaining noise class and the only one a path/keyword filter can't reach.

### skillsmith  (v1.1.4 → 1.1.5, status lean, mentions 620, days-since 0.1)
- STORM scan (5 personas, collapsed — corpus unchanged since Run 1's fan-out): Daily-Driver = "frictionful"
  list still unusable, every entry I open is a worktree doc / catalog echo / the routine's own prompt;
  Red-Teamer = guard against over-exclusion → satisfied, the fix removes ONLY catalog/`.claude/`/self-prompt
  sources, user-role prose survives; Architect = one-file PATCH, extend `SKIP_DIRS` + the `message_texts`
  drop + a per-line guard, no API change; Archaeologist = 3rd run in a row on the same defect, close it;
  Economist = highest ROI, unblocks every future per-skill STORM.
- Contradiction map: agreed (all 5) = the three sources are non-usage and safe to exclude · blind-spot =
  topical word-collision (class B), not reachable by path exclusion.
- Synthesis: bundle the three exclusions as one correctness PATCH (v1.1.5); leave class B as the next-best
  proposal.
- Peer review: **conf 9/10**. Weakest link = the catalog/self-prompt drops rely on stable English marker
  strings; if the harness reworded the catalog header it'd regress (verify path: re-run scan, confirm
  `vault-log-compliance` / `cleanup` / `supabase` / `skillsmith` friction collapses to genuine-or-empty).
  Bias = Economist/Archaeologist (ROI/recurrence) lead, but Red-Teamer's over-exclusion guard holds.
- Decision: **APPLIED** — §R4.5 cooldown waived (correctness, consistent with Run 1 & 2 today); self-edit
  last + `check skillsmith` gate + device re-sync.

## Run 2 Summary
- **Applied:** `skillsmith` PATCH → **v1.1.4** [conf 8/10] (commit pending) — `routine_scan.py`
  **source-class friction filter**, resolving Run 1's deferred next-best proposal. Excludes skill/command
  **definitions** (`/.claude/skills|commands/`), **config perms** (`settings*.json`), **log indexes**
  (`vault-logs/INDEX.md`); drops per-line **"consulted/delegate/invoke the X skill/per X-workflow/read
  CLAUDE.md"** boilerplate; and drops **session-transcript skill-body injections** (`Base directory for
  this skill:` messages — 185 of them, the dominant residual). Verified by before/after: the
  all-definitional baseline (cleanup 6 own-DONE-items, skillsmith 6 self-changelog, supabase `.claude`
  matches, transactions/vault-log delegate-boilerplate) is gone. Gate `check skillsmith` = 0 violations;
  device synced 1.1.4.
- **Created:** none.
- **Deferred (NEW, surfaced by run 2 — for the next run):**
  - **(A) System available-skills-block description injection** [conf ~7/10 to-fix] — the harness
    `<system-reminder>` skill-list echoes each skill's *description* into the transcript; those lines
    carry friction keywords (e.g. `lex_cleanup`'s "fix dev errors") and still flag
    `vault-log-compliance` ×6. Different vector from the `Base directory:` header → needs its own marker
    (e.g. drop a user message whose text matches "The following skills are available" / a run of
    `name: …\ndescription: …` skill-card lines).
  - **(B) Topical / word-collision in app docs** [conf ~5/10, hard] — `supabase` (INTEGRATION/BACKEND
    docs about the product), `performance` (the English word + commit-type legend), `dev-server` (tsc/eslint
    "Verification:" boilerplate) match legitimate `docs/` prose. Distinguishing usage-friction from
    topic-mention needs a semantic gate (e.g. require co-occurrence with a request verb in a *typed*
    `('user', str)` message), not a path/keyword filter. Below the auto-apply bar; leave as research.
- **Cost:** direct scan diffing + 3 targeted edits to `routine_scan.py` (no STORM fan-out — Run 1 already
  STORMed the 8-skill active set this morning; the corpus is unchanged, so re-fanning would churn). Skills
  acted on: 1 (`skillsmith`). Roots: 3.
- **Next-best proposal:** ship deferred (A) — the system-skill-list-block injection filter — next run; it's
  the last cleanly-catchable definitional class. After that, only the hard topical class (B) remains.

## Run Summary
- **Applied:** `skillsmith` bug-fix → v1.1.3 [conf 9/10] — `routine_scan.py` over-counted quoted descriptions (left `\"`-escapes as 2 chars), producing a phantom `desc>1024` flag on `cleanup` (read 1025 vs the authoritative registry's 991). Mirrored `skill_registry.get_description`'s unescape. Verified: post-fix scan reads `cleanup` 991 / lean.
- **Created:** none.
- **Deferred:**
  - `conquering-campaign` body>10k (10290 words) [conf 7/10] — genuine soft Cowork WARN, but it's a fork and a lean-pass is risky-to-do-safely-autonomously polish, not correctness. Leave for a deliberate lean-pass.
  - `routine_scan.py` friction-detector noise [conf 7/10] — friction is keyword-matched near any skill *mention*, so it fires on the skills' own docs, vault-log "Docs consulted:" boilerplate, `settings.json` permission lines, and other skills' "delegate to X" instructions. Needs a source-class filter so "frictionful" means real usage friction.
- **Cost:** ~1 STORM workflow (8 named-skill graded-verdict agents, stopped early after the corpus read was already decisive) + direct registry/scan diffing. Skills STORMed: 8 (corpus-confirmed). Roots: 3 (repo + ~/Documents/Claude/Projects + session transcripts).
- **Next-best proposal:** add the friction-detector source-class filter (exclude skill-own-docs, vault-log boilerplate, permission lines, "delegate to" mentions) — it's the single change that would make the daily harvest's frictionful ranking trustworthy instead of noise-dominated.

## Harvest
- 29 skills · 14-day window · 0 dormant.
- active (top): supabase (638 mentions), cleanup (216), performance (115), vault-log-compliance (70), conquering-campaign (61), dev-server (57), bug-fix-workflow (23), transactions-domain-model (16), skillsmith (14).
- frictionful (13 by raw count): bug-fix-workflow, cleanup, conquering-campaign, db-rename-sweep, dev-server, graphify, image-to-code, impeccable, performance, skillsmith, supabase, transactions-domain-model, vault-log-compliance.
- new-skill hints (generic stop-words, no real gap): step / check / order / lint / council / docs / client / user.
- Status flags from the scan: `cleanup` desc>1024 (FALSE — counter bug, see Applied), `conquering-campaign` body>10k (TRUE), `graphify`/`image-to-code`/imagegen-* "large" (bodyW>5k, all soft/OK).

## Per-skill dossiers

### skillsmith  (v1.1.2 → 1.1.3, active, mentions 14, days-since 0)
- STORM scan: the skill's own self-tooling produced the only real finding of the run, not the skill content.
- Contradiction map: agreed = `routine_scan.py` `desc_chars` disagrees with the authoritative `skill_registry.py` gate · blind-spot = harvest trusted its own status labels.
- Synthesis: (1) unescape `\"` in the quoted-desc branch [APPLIED]; (2) source-class friction filter [DEFER].
- Peer review: conf 9/10 — fix is objectively verified against the canonical Cowork gate (991 both ways); weak link = only the inline-quoted desc branch was touched (block-scalar descs were already counted correctly, no skill uses an escaped block scalar today).
- Decision: **APPLIED** (PATCH, self-edit last, gate `check skillsmith` = 0 violations, device synced 1.1.3).

### cleanup  (v1.17.1, status lean after fix, mentions 216, days-since 0)
- STORM scan: 6 "friction" snippets — all are the skill's own UPGRADE-NOTES/ROUTES "DONE" items + skillsmith's cowork-limits doc. Zero real usage friction.
- Peer review: conf 9/10 NO-OP. Cooldown anyway (changed today, v1.17.1).
- Decision: **NO-OP** — the only cleanup-related signal (`desc>1024`) was a skillsmith counter bug, not a cleanup defect. Registry confirms 991 ✓ lean.

### conquering-campaign  (v3.8.2, status body>10k, mentions 61, days-since 4.6, FORK)
- STORM scan: friction = its own failure-mode catalog + cleanup's consolidation doc. No usage friction.
- Synthesis: lean-pass the 10290-word body under 10k by moving recipe prose to `references/` (never cut routing logic).
- Peer review: conf 7/10 to-defer — real soft WARN, but fork-aware lean-pass is a careful manual job; auto-trimming a 467-line campaign skill risks dropping routing logic. Below the ≥8 auto-apply bar.
- Decision: **DEFERRED**.

### supabase (v0.1.2, mentions 638) · performance (v1.0, 115) · vault-log-compliance (v1.0.0, 70) · dev-server (v1.0.0, 57) · bug-fix-workflow (v1.0.0, 23) · transactions-domain-model (v1.0.0, 16)
- STORM scan (collapsed): every friction snippet for these six is doc/vault-log/permission/delegate-to keyword noise — `supabase` matches the `cleanup-routine` command + codex config + cleanup's postgres-mode rows; `performance` matches INTEGRATION/FRONTEND docs + a commit-type legend; `vault-log-compliance` matches other skills' "delegate to vault-log-compliance" lines; `dev-server`/`bug-fix-workflow`/`transactions-domain-model` match vault-log "Docs consulted" boilerplate + session "Start: read CLAUDE.md…invoke skill" boilerplate.
- Peer review: conf 9/10 NO-OP across all six (independently confirmed by the STORM workflow: every completed verdict returned `friction_real=false / NO-OP / 9–10`).
- Decision: **NO-OP** — no real friction; high mention counts are healthy usage, not pain.

### new-skill candidates
- Gap signal: none real. Hints (step/check/order/lint/council/docs/client/user) are generic tokens, already covered by existing skills (cleanup `lint`, etc.).
- Decision: **DEFERRED** (nothing to create).

## Note for the next run
- The harvest's `frictionful` list is currently noise-dominated; until the source-class filter lands (deferred above), rank candidates by *registry status flags* + manual snippet inspection, not by raw friction count.
- The STORM Workflow over-explored (default agent type explored the repo despite a "judge from provided context only" instruction, ~25 min for 8 agents). Next run: give the STORM agents a tighter, read-bounded prompt or a lightweight agentType so they grade from the payload without filesystem walks.

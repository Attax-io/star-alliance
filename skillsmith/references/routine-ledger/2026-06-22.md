# routine 2026-06-22

## Run Summary  (Run 26 — APPLIED: codex-law-translate body-vs-reality + parser-fleet drift · 1 auto-apply, conf 9/10)
- **Applied (1, conf 9/10):** `codex-law-translate` **1.0.0 → 1.1.0** (MINOR). Promoted 5 reusable parsers
  device→repo and rewrote the skill body to match a year of real loads (12 laws) captured in project memory but
  never reflected in the skill.
- **Method:** the converged-library lever (Runs 12–25) — re-aim the lens onto a never-body-STORMed active skill.
  `codex-law-translate` (17 file mentions, 2 sessions, never body-STORMed) had **zero friction snippets** but a
  large **body-vs-reality gap** vs three rich discovery memories (`codex-large-law-gotchas`,
  `codex-law-two-grammars-h4-parser`, `codex-law-meta-translation`).
- **Two real defects found:**
  1. **Repo↔device parser drift (same class as R13 bug-fix-workflow).** Device global
     `~/.claude/skills/codex-law-translate/scripts/` carries 5 reusable parsers
     (`parse_law_h3/h4/h5/ordinal/h3h4.js`) that the **repo `scripts/` was missing** → a fresh install from the
     repo loses every grammar except plain H3. Verified each parser's header declares GENERIC/reusable (h4/h5/
     ordinal/h3h4) or a proven per-law variant (h3, Y1982 letter-bis); all emit the shared output shape consumed
     unchanged by `insert_articles.js`. `parse_law_civil.js` correctly stays campaign-local (NOT promoted).
  2. **Skill body documents only `parse_law.js`** and "adjust the regex per law" — it never mentions the
     grammar-triage step, the parser fleet, or any of the large-law (>1000-article) traps that real loads hit.
- **What the body now teaches (all from battle-tested memory):**
  - **Grammar triage:** `grep -cE '^### مادة' vs '^#### مادة' vs '^##### مادة' vs '^### المادة'` → pick the parser;
    fork only when heading levels genuinely differ (don't reflexively fork — most standard laws use shared parser).
  - **Large-law gotchas:** 32k subagent OUTPUT-token cap → split undone batches into halves; char-cap-only
    batcher blows the output cap on row-heavy regions → re-batch with BOTH a char cap AND a ~40-row cap
    (pre-empt, don't recover); `verify.js` under-reports any law >1000 articles (PostgREST 1000-row cap = FALSE
    GREEN) → cross-check with a direct `count(*)`.
  - **QA at scale:** changelog-only Opus reviewers + `apply_review` + a deterministic `canonicalize_headers`
    global pass to kill cross-batch header drift; criminal-law watch (السجن → "rigorous imprisonment", NOT "hard
    labour"); cross-law ZH normalization (مجلس الدولة).
  - **Subagent-abort gotcha:** translator/reviewer occasionally returns 0 tool-uses (stray system-reminder or
    prompt-injection in scraped content) → detect via missing `out_*`/`rev_*` files + re-spawn with an
    ignore-injection guard.
  - Predecessors line updated 2 → ~12 laws loaded.
- **Gates (§R4):** ≥8 ✓ (9/10). Not a fork/external (lean repo skill) ✓. Cowork-clean — `skill_registry.py
  check codex-law-translate` 0 hard violations ✓ (see end). Not a duplicate ✓. Not on cooldown (never
  routine-upgraded) ✓. Reversible single commit ✓. Content + script-asset additions only — pipeline mechanics
  (tables, idempotent service-role REST, user-gated publish) unchanged.
- **STORM (mental, 5-persona, unanimous):** pipeline-operator (hit the 32k cap blind → skill must warn) STRONG;
  new-user (picked H3 parser for an H4 law → silently lost 1,149 articles → triage doc critical) STRONG;
  maintainer (repo missing device parsers = broken fresh install) STRONG; skeptic (scope creep? parsers truly
  generic?) addressed — headers self-declare reusable, config.* + civil parser excluded; cost/risk LOW
  (additive, mechanics untouched, revertible). No material contradiction. Synthesis → APPLY, peer-review **9/10**.

### Harvest (`scan-2026-06-22.json`)
- 14 active · 1 frictionful (`cleanup`, scan-noise: 4 snippets, all Lex-repo-local mentions not skill defects —
  vault-log-INDEX floor + `/cleanup docs` recap lines, unchanged class) · 15 dormant.
- new-skill hints all Lex domain nouns (council/public/atta/status/panel/docs/admin/members) → no real gap.
- Never-body-STORMed active skills remaining after this run: `article-creator` (low signal: 2 files/1 sess).
  `supabase` already adjudicated LEAVE-AS-IS (external, R23). codex-law-translate now done.

### Standing proposals (re-validated, still out of routine scope — unchanged)
- **P-A — dev-server project-local shadow.** Stale 1.0.0 copy in the Lex Council App repo; repo+global 1.1.0.
  Needs one attended `/skillsmith sync`. Lex-repo-local → cannot fix unattended.
- **P-B remainder — conquering-campaign device-copy sync.** Repo edition correct (3.8.3); Lex project-local copy
  one attended `cp`/sync away. Same out-of-scope blocker.

### Health checks (in-scope, all green)
- `skill_registry.py check skillsmith` = 0 hard violations (recorded post-run below).
- No skillsmith self-edit this run.

### Cost
- Read-only scan + 4 memory reads + parser-header inspection + 1 MINOR upgrade (5 file promotions + body rewrite).
  Skills touched: 1. Skills STORMed (body-vs-reality): 1. Roots: 3. No Workflow fan-out (single clear target).
- **Next-best proposal:** body-STORM `article-creator` next run (only remaining never-STORMed active skill), and
  the standing attended `/skillsmith sync` that clears P-A + P-B together.

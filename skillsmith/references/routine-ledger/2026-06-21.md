# routine 2026-06-21

## Run Summary  (Run 21 — CLEARED a standing 8/10 proposal: transactions-domain-model per-name v2 rewrite)
- **Applied:** `transactions-domain-model` **1.1.0 → 1.2.0** (conf **9/10**). **Created:** none. **Fixed:**
  the standing *defect* that 1.1.0 only flagged — the skill presented 8 pre-v2 DB-object names as if live.
- **What changed the calculus:** every prior run (13–20) deferred this proposal with the *same* blocker —
  "needs a live DB read to verify each v2 successor." **This run had the Supabase MCP available**, so the
  blocker was removable. Read-only `information_schema` probe (project `bqgrpnsvplvicnmzxwkm`) proved drift:
  - **DEAD (all 8):** `transactions_personal_js`, `transactions_pending_js`, `transactions_approval_js`,
    `transactions_certification_js`, `transactions_safe_js`, `transactions_history_js`,
    `transactions_container_js`, plus `fd_access_js`, `branches_js`, and the `transaction_templates` table.
  - **LIVE successors (verified present):** `members_finances_transactions_personal`,
    `admin_finances_transactions_{pending,approval,certification,safe,history}`,
    `admin_files_transactions_detail`, `folder_access`, `branches`, `templates`/`admin_templates_list`.
  - **STILL-LIVE (kept, not touched):** trigger `tr_on_tr_b_iu` + columns `action_required_user_inquestion`,
    `done_by`, `transaction_to`, `payment_for_fees`, `on_customer_by_agreement`.
- **Why ≥8/10 despite cooldown:** stale schema names a session would query are a **correctness bug**, and the
  §R4 correctness override permits a cooldown-window fix when a *real defect* is proven (it was, by live DB).
  Confidence 9/10: the dead/live split is schema-proven, not inferred; the only soft spot (held back from 10)
  is the tab→view 1:1 *semantic* mapping for Personal/Pending (member vs admin portal split) and the retired
  "45-column zero-drift" claim — both now flagged in-skill for column-shape re-check rather than asserted.
- **Upgrade-playbook ran clean:** `skill_registry.py check` (0 hard violations) → `write` (VERSIONS.md → 1.2.0)
  → `skill_sync.py apply` (device global now 1.2.0). Self-edit guard N/A (not a skillsmith self-edit).
- **Deferred (carried):** article-creator admin-read-policy mechanism [8/10, prior agent verification FAILED —
  needs live `pg_policy` read, not attempted this run]; bug-fix-workflow status-2 live label [6–7/10 < gate].
- **Cost:** 3 read-only SQL probes + 7 skill edits + registry/sync. **Skills STORMed:** 1 (transactions, the
  decisive lever) + corpus byte-stable vs Run 20 (12,384 B, no new friction class → prior verdicts carry).
  **Push:** see end-of-ledger.

---

## Run Summary  (Run 20 — fresh day, ran the standing sync-drift lever; no ≥8/10 action, by design)
- **Applied:** none. **Created:** none. **Fixed:** none.
- **Ran the one un-executed cheap non-DB lever** the Run-13 summary named as a standing proposal
  (the *sync-drift guard*, conf 7): a full **repo↔device version-parity check** across all 29 skills,
  because a real repo/device drift is a *bug* (correctness) that overrides the §R4.5 cooldown. Result:
  the **only** divergence is `cleanup` repo **1.18.0** / global **1.17.0** — and that is the
  **documented, deliberate FORK divergence** (repo = lean Cowork edition, device = full canonical
  monolith; Run 12), with the repo *ahead*. This is the **opposite** of the Run-13 defect class
  (device-ahead-of-repo, repo lost a hand-upgrade). §R4.2 forbids blind-overwriting a fork in either
  direction, and the sync scripts deliberately `SKIP` forks on `apply`. `conquering-campaign` (3.8.2)
  and `impeccable` (3.0.7) show `—` for global = fork / npx-external, both expected. **No real
  sync-drift defect → no action.** The lever is now run and joins the converged set.
- **Deferred (all carried, unchanged):** the three DB-gated standing proposals —
  transactions-domain-model per-name v2 rewrite [8/10, needs `information_schema`],
  article-creator admin-read-policy mechanism verification [8/10 agent → failed verification, needs
  live `pg_policy`], bug-fix-workflow status-2 live label [6–7/10, needs prod read]; plus the friction
  lever's lone live lexical candidate (cleanup leaks-mode `agent()` dispatch logged as plain user turn)
  at **7/10** < §R4.1 gate. None doable on an unattended, non-DB run.
- **Cost:** ledger + scan + sync-status only (no skill change). **Skills STORMed:** 0 fan-out (corpus
  byte-stable vs converged baseline → prior verdicts carry; the new work this run was the parity lever,
  decisive without fan-out). **Roots:** 3.
- **Next-best proposal:** a **DB-enabled** run clears all three standing proposals at once. Absent that,
  wait for a genuinely NEW corpus signal (fresh session friction / a new machine-dispatch class the
  v1.1.7 structural filter misses). An unattended skills-repo run can provide neither.

## Harvest
- **active (15):** supabase, cleanup, performance, dev-server, bug-fix-workflow, conquering-campaign,
  transactions-domain-model, codex-law-translate, db-rename-sweep, graphify, skillsmith, article-creator …
- **frictionful (1):** `cleanup` (2 snippets). **dormant (14):** design/viz/imagegen + obsidian etc.
- **Corpus change vs 2026-06-20 baseline:** scan shrank **12,828 B → 12,384 B**. The one delta is
  **`dev-server` aged OUT of the frictionful set** (2 frictionful → 1): its sole prior snippet was a
  `MEMORY.md` standing-instruction index line, not a defect — it simply fell outside the 14-day window.
  No NEW friction class appeared; the corpus only *lost* a non-defect line.
- **`cleanup` friction snippets (2)** = the documented **accepted-noise tail** (Runs 5–19):
  1. `"3) • MED — ~540 vault-logs missing from INDEX… + ~1299 broken .md wikilinks… systemic class…"`
     — pasted analysis-report body, no structural marker / reliable lexical prefix; skill-side already
     fixed Run 6 (cleanup v1.18.0 docs systemic-threshold escape). Permanent floor (Run 5 ruling). NO-OP.
  2. `"Task: the summary card on the members finances page renders placeholder text… i18n keys USED…
     but ABSENT…"` — a Workflow `agent()` leaks-mode dispatch logged as a plain user turn. The lone
     live lexical candidate; still **7/10**, below §R4.1; cleanup on cooldown + fork. Deferred, unchanged.
- **new-skill hints:** council×62, public×42, status×41, atta×40, panel×32, deploy×27, admin×26,
  docs×26, start×25, apps×25, read×23, members×23 — **Lex app-domain nouns** (20 runs consistent), not
  cross-project tool gaps. No new-skill candidate.

## Cooldown census (§R4.5, 3-day window)
All 8 skills upgraded by the routine on 2026-06-20 remain on cooldown until ~2026-06-23:
bug-fix-workflow (1.1.1), cleanup (1.18.0), db-rename-sweep (1.1.0), dev-server (1.1.0),
performance (1.0.1), skillsmith (1.1.7), transactions-domain-model (1.1.0), vault-log-compliance (1.1.0).
No fresh *bug* found in any to invoke the correctness override.

## Per-skill / lever dossiers

### sync-drift parity lever  (NEW this run — executed the Run-13 standing proposal, conf 7)
- Scan: `skill_sync.py status` over all 29 skills. Sole divergence = `cleanup` repo 1.18.0 / global 1.17.0.
- Contradiction map: agreed = a device-ahead-of-repo drift is a real correctness bug (Run 13 class,
  overrides cooldown) · blind-spot = a FORK's repo-ahead divergence is *intended*, not drift.
- Synthesis: the `cleanup` gap is the documented fork (repo lean edition ahead; device full monolith
  1.17.0). §R4.2 → never blind-overwrite a fork; scripts SKIP forks on apply. `conquering-campaign` /
  `impeccable` `—` global = fork / npx-external. Zero genuine drift.
- Peer review: conf 9/10 — verdict objectively grounded in the version table + the documented fork
  edition split (Run 12); weakest link = none, the parity table is mechanical fact.
- Decision: **NO-OP** — no device copy is ahead of its repo canonical; the one gap is the deliberate
  cleanup fork. Lever now run and converged; joins friction lever + body-vs-reality ladder + cross-skill
  consistency as the four executed-and-converged levers.

### everything else  (body-vs-reality ladder · cross-skill consistency · friction lever)
- All run and converged across Runs 12–19; corpus byte-stable vs that baseline (minus the dev-server
  non-defect shrink) → no new evidence, prior verdicts carry.
- Editable non-cooldown candidates this run: `article-creator` (adjudicated Run 16 — deferred-only
  proposals, no new signal), `codex-law-translate` (Run 16 NO-OP healthy), generic vendored design/viz
  skills (no Lex project-reality contradiction possible), `supabase` / `supabase-postgres-best-practices`
  (vendored/external → refresh-only, banned on an unattended run). `conquering-campaign` fork @ 7/10 < gate.
- Decision: **NO-OP** — nothing clears ≥8/10 + §R4.

## Why no-op is correct (Run 20)
Four convergence levers (friction · body-vs-reality ladder · cross-skill consistency · sync-drift parity)
are now all run and converged. Every standing proposal is DB-gated or below the gate; every routine-touched
skill is on the 3-day cooldown with no fresh bug; forks/externals are refresh-only and unsafe to touch
unattended. Forcing any action would violate §R4.1 / §R4.5 / §R4.2. Correct output = no edit + a watchable trail.
